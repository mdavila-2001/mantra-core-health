# Fixtures del E2E de carga masiva

Copia byte a byte de los generados por `test/fixtures/terminology-import/generar-fixtures.mjs`
en `mantra-core-health-api`. **Sintético, sin procedencia externa, prefijo `ZZ-`.**
Verificado con `sha256sum` contra el original.
`grande.csv` (11 MiB, sólo para el test de límite de tamaño) se genera aparte y **no se commitea**.
