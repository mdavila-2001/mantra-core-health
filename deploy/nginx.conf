# Reverse proxy de producción: la API detrás del MISMO dominio que el frontend.
#
# Es la decisión de arquitectura tomada, y la que hace que:
#
#   · `PUBLIC_API_BASE_URL` quede **vacía** — las peticiones salen relativas;
#   · **no haga falta CORS**, porque el navegador ve un solo origen;
#   · la CSP se quede en `connect-src 'self'` y no cambie nunca.
#
# Este archivo es una **referencia funcional**, no una imposición: si el
# despliegue usa un Ingress de Kubernetes, un ALB o Caddy, lo que hay que
# reproducir son las dos reglas de abajo. Lo que no es negociable es que los
# prefijos lleguen a la API y todo lo demás al servidor de renderizado.
#
# ─── Los prefijos ────────────────────────────────────────────────────────────
#
# Salen de `proxy.conf.json`, que es lo que usa el desarrollo. **Las dos listas
# tienen que decir lo mismo**: un módulo nuevo de la API que se agregue en un
# lado y no en el otro funciona en desarrollo y falla en producción — o al
# revés, que es peor porque nadie lo prueba. Lo verifica
# `scripts/check-api-prefixes.mjs` en CI.
#
#   /iam  /public  /terminology  /profiles  /identity  /common
#   /scheduling  /charts  /clinical  /authz
#   /practitioner-delegates  /access-requests  /delegated-access
#   /delegated-permission-sets  /org  /auth-providers
#   /admin/tenants  /tenants
#   /health-context  /geo
#   /practices  /accounting
#
# Del primer bloque, los cuatro últimos entraron con las secciones de
# **Atención**: agenda (`/scheduling`) y archivo clínico (`/clinical` +
# `/charts`), más las bases legítimas de acceso de la ficha de paciente
# (`/authz`). El segundo bloque entró con acceso delegado (M29) y proveedores
# de identidad (M40); `/admin/tenants`, con organizaciones (M04); los dos
# últimos, con contexto sanitario (M44) y geolocalización (M13). `/tenants` con
# la ficha de organización, y `/practices` + `/accounting` con la sección
# contable: los libros cuelgan de una práctica, así que los dos van juntos o no
# va ninguno.
#
# Cada prefijo tiene que ser **inequívoco frente a las rutas del router**, que
# comparten origen. Ya mordió una vez en desarrollo: agregar `/admin` para
# `GET /admin/tenants` desviaba `/administracion/pacientes` —una ruta de la
# aplicación— hacia la API, que respondía `Cannot GET`. Por eso ese prefijo es
# **el único de dos segmentos**: es la forma de que no sea ambiguo.
#
# ─── Y un séptimo prefijo que NO es la API ───────────────────────────────────
#
# `/otel` recoge las trazas del navegador y va al **servidor de renderizado**,
# no a la API: `src/server/telemetry/otel-gateway.ts` las reenvía al Collector,
# que vive en la red interna y no se publica. El navegador manda al mismo origen
# justamente para que la CSP se quede en `connect-src 'self'`.
#
# Está declarado aparte, y no cubierto por el `location /` de abajo que ya lo
# alcanzaría, porque es el sitio donde van los dos controles que ese endpoint
# necesita: el límite de tasa y el de tamaño. No está autenticado y no puede
# estarlo — cualquier credencial que usara el navegador viajaría en el paquete
# que descarga todo el mundo.

upstream api {
    # El servicio de la API. En Docker Compose es el nombre del servicio.
    server api:3000;
    keepalive 32;
}

upstream frontend {
    # El servidor SSR de este repositorio (`dist/…/server/server.mjs`).
    server web:4000;
    keepalive 32;
}

# Límite de tasa de la telemetría, por dirección de origen.
#
# Un navegador exporta un lote cada cinco segundos, o sea 12 por minuto y por
# pestaña. 60r/m deja margen para varias pestañas abiertas y sigue cortando en
# seco a quien intente usar el endpoint como sumidero. 10 MB de zona guardan del
# orden de 160 000 direcciones.
limit_req_zone $binary_remote_addr zone=otel:10m rate=60r/m;

# ─── Validación del `Host` ───────────────────────────────────────────────────
#
# Está acá y no en Angular por una razón concreta y comprobada: la opción
# `security.allowedHosts` del build **se hornea en el artefacto**, así que
# enumerar el dominio ahí significaría una imagen distinta por dominio, y
# cambiar de dominio obligaría a reconstruir.
#
# Peor todavía: el valor que traía el proyecto era `["localhost"]`, y eso hacía
# que el servidor de producción **rechazara con 400 cualquier petición de un
# dominio real**. Lo detectaron las pruebas de extremo a extremo corriendo
# contra el artefacto construido; no se veía de ninguna otra forma.
#
# El control se pierde poco al moverlo: la protección existe contra un `Host`
# manipulado que lleve al servidor a pedirle algo a un host interno, y **este
# servidor SSR no hace ninguna petición saliente** — no habla con la API.
#
# Acá, en cambio, es configuración en caliente: se cambia el dominio y se
# recarga nginx.
server {
    listen 80 default_server;
    server_name _;

    # Un `Host` que no reconocemos ni siquiera llega al servidor de
    # renderizado. 421 es el código correcto: «esta petición no es para este
    # servidor».
    return 421;
}

server {
    listen 80;

    # ⚠️  CONFIGURACIÓN: acá va el dominio real. Mientras diga `localhost`, solo
    #     responde en local. Es lo único de este archivo que hay que tocar.
    server_name localhost mantra-core-health.local;

    # Un documento clínico puede pesar. El límite real lo pone la API; éste solo
    # evita que una subida absurda llegue hasta ella.
    client_max_body_size 12M;

    # ─── 1 · La API ─────────────────────────────────────────────────────────
    #
    # La lista vive en `api-locations.conf`, compartida con el proxy del
    # despliegue en Coolify (`nginx.coolify.conf`). Mantenerla en un solo
    # sitio es lo que impide que los dos servidores enruten distinto.
    include /etc/nginx/api-locations.conf;

    # ─── 2 · Las trazas del navegador ───────────────────────────────────────
    #
    # Va al servidor de renderizado, NO a la API. El `location /` de abajo ya lo
    # alcanzaría; existe aparte por los tres controles que aplica, que son lo
    # único que protege un endpoint que no puede autenticarse.
    location ^~ /otel/ {
        # Solo POST. Un GET acá no tiene ningún significado.
        limit_except POST { deny all; }

        limit_req zone=otel burst=20 nodelay;

        # El mismo tope que impone el gateway (`MAX_BODY_BYTES`). Declararlo acá
        # además evita que un cuerpo enorme llegue siquiera a Node.
        client_max_body_size 512k;

        proxy_pass http://frontend;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection        "";

        # La telemetría nunca es lo urgente: si tarda, se corta.
        proxy_read_timeout 10s;
    }

    # ─── 3 · Todo lo demás: el servidor SSR ─────────────────────────────────
    #
    # Incluidos los estáticos: `src/server.ts` ya los sirve con `maxAge: 1y`, y
    # **emite las cabeceras de seguridad**. Servirlos desde nginx directamente
    # las perdería, salvo que se repitan acá — y dos fuentes para la misma
    # política es cómo se separan.
    location / {
        proxy_pass http://frontend;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection        "";

        # El prerenderizado responde rápido; el resto también. Un timeout largo
        # solo esconde un problema.
        proxy_read_timeout 30s;
    }
}
