# Plan — Tarea 2: WhatsApp y Call Center de aseguradora (certificación E2E y cierre de brechas)

- Fecha: 2026-09-24 · Repos afectados: `mantra-core-health` (rama `marcelo/feat-insurance-whatsapp-callcenter`, desde `origin/dev`), `mantra-core-health-api` (rama `marcelo/feat-insurance-whatsapp-callcenter-api`, desde `origin/dev`; **un solo cambio: el mínimo de dígitos E.164 del DTO**, ver H1.S1), y `mantra-core-health` rama `marcelo/feat-insurance-whatsapp-callcenter-mockup` desde `origin/mockup` (porte decidido). Modelo y bóveda: **sin cambios** (las tres columnas existen desde v4.2.10).
- Predecesor: Subtarea 2.3 «canales de contacto directo» (modelo #18 · bóveda #72 · API #395 · front #446, **todos mergeados a `dev` el 2026-09-13**) y Tarea 1 «portabilidad a 1 clic» (front #638 → `dev`, #639 → `mockup`, API #455 → `dev`, **los tres MERGED el 2026-09-24 a las 23:10–23:14Z**).
- Fuente del requisito: `REGISTRO DE PROCESOS POR MODULO.md:492` (Proceso 2 · ítem 5: «llamar mediante Whatsapp directo a la compañía de seguro… desde su mismo número») = bóveda 6.2 · ítem 5 = `docs/tareas/subtarea-2.3-canales-contacto-aseguradora/`. El pedido lo llama «Subtarea 2.5»; se cita con las tres numeraciones.

## Contexto (qué descubrió la FASE 0)

**La feature ya está en `dev`, entera y con persistencia real (Subtarea 2.3).** Lo que el pedido manda «implementar» existe; la Tarea 2 es, como la Tarea 1, **certificación contra los CA nuevos y cierre de las brechas que esos CA destapan**:

| Capa | Lo que ya existe (ruta) |
|---|---|
| Modelo | `mantra-core-health-model/SQL/patches/2026-09-13_v4210_insurance_carriers_contact_channels.sql` (`whatsapp_number`, `call_center_phone`, `support_email`, nullable), aplicado a Neon (columnas 16 → 19), con backfill **sourced** de 7 aseguradoras reales (BISA `800-10-6060` :66-67, Fortaleza `800-12-9992` :76-77, La Boliviana Ciacruz `+59171548278` :95-96…). El `68d952d` posterior («v4.2.21/v4.2.22… con sus contactos», PR #30) **no toca los canales**: sus «contactos» son representante legal y gerencias. Bóveda: `E insurance.insurance_carriers.md` ya documenta las 3 columnas. |
| API | `PUT /insurance-carriers/:id/contact-channels` con `UpdateCarrierContactChannelsDto` (`backbone.dto.ts:372-395`): `@Transform` quita `[\s().-]` (espacios, paréntesis, puntos **y guiones**) antes de validar; `@Matches(/^\+[1-9]\d{6,14}$/)` → `+` obligatorio y **mínimo 7 dígitos**; `backbone.dto.spec.ts:166-230` ya cubre «sin +» (400 con «E.164»), «`+591 715-48278` → `+59171548278`», letras (rechazo), `null`, y la línea 800 del call center (no E.164). Lectura: `claims-read.service.ts:400-401` mapea `carrierWhatsappNumber/carrierCallCenterPhone` en la cabecera del reclamo (`claims-read.dto.ts:146-150`; spec `claims-read.service.spec.ts:487-500` con `+59171548278` / `800-10-6060` / null); `declared-coverages-reader.ts:188-189` los mapea en las coberturas del paciente (`profiles/dto/read-patients.dto.ts:442-445`; spec `declared-coverages-reader.spec.ts:105-108`). |
| Front · detalle | `features/insurance/insurance-claim-detail/insurance-contact-channels/` (ts/html/css/spec): `a[app-button] variant=primary size=sm target=_blank rel="noopener noreferrer" data-testid=btn-whatsapp-claim`; call center `a[app-button] variant=outline size=sm data-testid=btn-callcenter-claim` con `tel:`; correo `a[app-link]` `mailto:`; vacío honesto `insurance-contact-absent` «La aseguradora no registró canales de contacto.». Ruta real: `/administration/insurance-claims` → fila `claim-link` → detalle (confirmada por la suite E2E existente). |
| Front · tarjeta | `shared/components/molecules/patient-coverage-card/` (ts/html/css/spec; **no viene de la 2.3**: la agregó `7dc9c765` el mismo 13/09): WhatsApp como `a[app-link] [external]=true` (el átomo Link pone `target/rel`, `link.ts:42-43`), call center `a[app-link]` `tel:` con guard `dialable(phone)`, vacíos «WhatsApp: No informado» / «Call center: No informado»; CSS `.coverage__contacts a { min-height: 44px }` (`:62-66`, sin `min-width`); **sin `data-testid`** en los enlaces. Se monta en `/my-account` › «Seguros y tutores» (`my-profile.html:424-439`, una tarjeta por cobertura; datos de `GET /profiles/patients/me`, `ProfilesClient`). Ya la cubre el E2E `playwright/patient-coverage-copays.spec.ts` (`tel:800106060`, alto ≥ 44 del call center, desborde 1440/390, foco por Tab). Tipos: `profiles.types.ts:850-851` (`OwnCoverage.carrierWhatsappNumber?`, sin `carrierSupportEmail`), `insurance.types.ts:333-335` (`ClaimListItem`). |
| Front · utilidad | `shared/utils/telephone/telephone.ts`: `dialable()` (quita todo salvo dígitos y `+`), `whatsappDigits()` (solo dígitos), `whatsappUrl(raw, text)` → `null` si **< 7** dígitos; no rechaza letras (las quita). Spec con 4 casos. |
| Maqueta | Las aseguradoras viven en `core/mock/handlers/insurance.handlers.ts:101-175` (`ASEGURADORAS`, «ficticios, formato boliviano»): Seguros Andina S.A. (`+59170000101` / `800-10-0101`), La Vitalicia (**sin WA**, `800-10-0102`), Alianza Seguros (`+59170000103` / `800-10-0103`), Caja Nacional de Salud (sin WA, `800-10-0104`), Caja Petrolera de Salud (**sin ningún canal**). `/my-account` (`profiles.handlers.ts:315-329`): cobertura con `+59170011223` / `800-10-6060`. **No existe `core/mock/fixtures/insurance.fixtures.ts`.** |
| E2E | `playwright/carril-contacto-aseguradora.spec.ts` (8/8 el 13/09): maqueta → `CLM-2026-0177` (Alianza) asevera `href` con `wa.me/59170000103?text=`, texto decodificado con el código, `target/rel`, `btn-callcenter-claim` `tel:800100103`; `CLM-2026-0163` (Vitalicia) sin WA; desborde en 390/768/1440 con capturas; + 3 pruebas contra la API Neon (`PUT` 400/200/404). **No mide área táctil, ni teclado, ni el estado «sin canales», ni la tarjeta de `/my-account`.** |
| Docs | Plantilla de PLAN/REPORTE: `mantra-core-health/docs/trabajo/2026-09-24-portabilidad-aseguradora/`. Walkthrough 2.3 con 10 deudas declaradas; la **#7 es «PII en la URL de `wa.me`»** (el nombre del paciente viaja como query string a Meta «porque el CA lo exige… quitarlo es un cambio de una línea»). |

**Baseline a medir al arrancar (H1.S1.M1 / H1.S2.M1):** specs dirigidos de API y front + `yarn typecheck` en los dos repos, con salida literal en `evidencia/`.

**Brechas vigentes contra CA-2.1…2.5 y el DoD (verificadas en código por lectura directa, por un discovery de 5 dimensiones con refutación adversarial de cada brecha —61 sobreviven, 4 refutadas— y por una crítica de factibilidad del plan; ninguna medida en runtime todavía):**
1. **CA-2.5 — el botón de WhatsApp del detalle mide 32–40 px en móvil, nunca 44.** `size="sm"` → `--_btn-side: 32px` (`atoms/button/button.css:118-123`); solo `md`/`lg` dan `min-height: 44px` bajo 780 px (`:125-134`); `src/styles/alovida.css` además fija `height: var(--h-ctrl)` (40 px) a todo `[app-button]`, que `min-height` 44 sí vence pero 32 no. Mismo defecto que la Tarea 1 corrigió en portabilidad (brecha 8 de su plan). En la tarjeta el enlace ya tiene `min-height: 44px` pero **sin `min-width`** ni medición del WhatsApp.
2. **CA-2.2 — el testid del call center es `btn-callcenter-claim`**, el CA exige `btn-call-center-phone`.
3. **CA-2.4 — `whatsappUrl` corta en 7 dígitos y no descarta letras** (`telephone.ts:49`); el CA exige `null` con < 8 dígitos o con letras. El spec de la utilidad (`telephone.spec.ts:27-30`, solo `'123'` y `''`) no cubre letras, guiones, prefijos internacionales, el borde 7/8 ni mensaje vacío (DoD front): el caso de 7 dígitos es **nuevo**, ninguno cambia de sentido.
4. **CA-2.1 — el mensaje prellenado no es el literal del CA** («Hola {aseguradora}, me comunico desde AloVida sobre la solicitud {código} del paciente {nombre} (Póliza {póliza})…» vs «Consulta sobre solicitud <CODIGO> - Paciente: <NOMBRE> - Aseguradora: <NOMBRE_ASEGURADORA>»). **Decisión del usuario (2026-09-24): se conserva el texto actual de la 2.3 y solo se certifica** que nombra solicitud, paciente y aseguradora; no es brecha a corregir. La deuda #7 de la 2.3 (nombre en la URL) sigue declarada.
5. **CA-2.1 — «Espacio» no activa un `<a>`** (solo Enter lo hace de forma nativa); ningún spec ni E2E prueba teclado.
6. **CA-2.1 — la tarjeta de cobertura no tiene `data-testid`** en sus enlaces (solo la raíz `patient-coverage-card`): el E2E existente los ubica por rol y nombre; no hay «equivalente» de `btn-whatsapp-claim`/`btn-call-center-phone`.
7. **CA-2.3 — el E2E no ejercita «sin ningún canal»**: la maqueta tiene a Caja Petrolera (índice 4 de `ASEGURADORAS`) pero **los 6 reclamos usan los índices 0, 1 y 2** (`insurance.handlers.ts:453-548`); hay que agregar un reclamo.
8. **CA-2.4 — no hay prueba que impida que un campo clínico entre al texto de la URL**: la cabecera (`ClaimListItem`) hoy no trae diagnóstico ni medicación, pero el `computed` no está blindado por contrato ni por test.
9. **DoD E2E — no existe `playwright/carril-insurance-whatsapp.spec.ts`**; existe la suite de la 2.3 con nombre en castellano (lane-29) que cubre la mitad de los casos.
10. **DoD backend — ya cubierto salvo el umbral.** El DTO normaliza guiones/puntos/paréntesis y `backbone.dto.spec.ts` existe con los casos del DoD; lo único que diverge del CA es el **mínimo de 7 dígitos** (`\d{6,14}`). **Decisión del usuario: 8 dígitos en las dos capas** → la regex pasa a `^\+[1-9]\d{7,14}$` (único diff de la API) y el front corta en 8.
11. **lane-29 —** nombre de la suite E2E en castellano; identificadores nuevos en inglés (el `computed` del mensaje no se toca, A1).
12. **CA-2.3 — `callCenterHref` puede emitir `tel:` vacío**: `insurance-contact-channels.ts:63-66` comprueba `phone` y no `dialable(phone)`; con un valor sin dígitos (`N/A`) queda un enlace roto. La tarjeta sí lo guarda (`patient-coverage-card.ts:37`).
13. **Regla 00 §2 — la maqueta de `/my-account` usa un número real**: `profiles.handlers.ts:315-329` fija para toda cobertura `+59170011223` / `800-10-6060`, y **`800-10-6060` es el call center real de BISA** (patch v4210:66-67), sin marca de ficticio; además Seguros Andina queda con dos juegos de números (reclamos `+59170000101`/`800-10-0101` vs tarjeta). Lo mismo pasa con los valores del DoD (`+59171548278` = La Boliviana Ciacruz, `800-12-9992` = Fortaleza): **no se adoptan** (A2).
14. **Arnés — la suite existente usa `estable()`** (`support/sesion.ts:142-149`, espera `networkidle`, que el `CLAUDE.md` del front prohíbe con `ng serve`), no filtra el ruido de CSP, escribe capturas en ruta fija y no fija viewport en los tests sin `setViewportSize` (el project `devices['Desktop Chrome']` de `playwright.config.ts:54` fuerza 1280×720 sobre el 1440×900 global).

**Desvíos del prompt (el repo desmiente):** el DoD pide `fixtures/insurance.fixtures.ts` con «Seguros Andina S.A.: WA `+59171548278` + CC `800-10-6060`, Alianza Seguros: sin WA, CC `800-12-9992`, Aseguradora Sin Contacto» — en la maqueta las aseguradoras viven en el handler, **Alianza SÍ tiene WhatsApp** (la de «solo call center» es La Vitalicia y la «sin contacto» es Caja Petrolera) y los números son otros (`+59171548278`/`800-10-6060` son los del **spec unitario de la API**, no los del mock); el E2E existente asevera `wa.me/59170000103` en `CLM-2026-0177`, el DoD pide `wa.me/59171548278` en el mismo reclamo. **El DoD se contradice a sí mismo**: pide abrir `CLM-2026-0177` y aseverar `wa.me/59171548278`, pero 0177 es de Alianza (`insurance.handlers.ts:515-516`) y el propio DoD dice que Alianza no tiene WhatsApp. **La navegación directa por código no existe**: el parámetro real es `:claimId` = UUID (`app.routes.ts:683`; maqueta `uuid('claim-CLM-2026-0177')`, `insurance.handlers.ts:554`; API `ParseUUIDPipe`, `claims-read.controller.ts:93`): `/administration/insurance-claims/CLM-2026-0177` da 404 en la maqueta y 400 en la API, así que el E2E entra por el listado como la suite de la 2.3 (cláusula «o la ruta viva» del DoD). **`carrierName` difiere entre maqueta y API**: el mock manda el nombre corto (`insurance.handlers.ts:614`, «Alianza Seguros»), la API la razón social (`claims-read.service.ts:398`). **El detalle de reclamo es de operador** (`@Roles('BILLING_OPERATOR','SECURITY_ADMIN')`, `claims-read.controller.ts:47`); la superficie del paciente que describe el stakeholder es la tarjeta de `/my-account`. Los componentes se llaman `InsuranceContactChannels`/`PatientCoverageCard` (convención del CLI, sin sufijo). El CI del front está caído: los `scripts/check-*.mjs` se corren a mano; `gh pr merge` está bloqueado (el flujo termina en abrir el PR).

**Ambigüedades registradas (regla 00 §1.7) y lo decidido con el usuario el 2026-09-24:**
- **A1 · Nombre del paciente en la URL de `wa.me`.** CA-2.1 lo exige; la regla 00 §2.3 / 90.2.2 prohíbe PII en URLs y la 2.3 lo dejó como deuda #7. **Decidido: se conserva el mensaje actual de la 2.3** (solicitud + paciente + aseguradora + póliza) y solo se certifica. El conflicto con la regla 00 §2.3 queda **asumido y registrado** en PLAN y REPORTE; lo que sí se blinda es CA-2.4 (ningún dato clínico entra al texto).
- **A2 · Valores de la maqueta.** **Decidido: conservar las 5 aseguradoras y números ficticios ya declarados** en `insurance.handlers.ts`; el E2E asevera los valores reales del mock (`wa.me/59170000103` para Alianza en `CLM-2026-0177`; Vitalicia `CLM-2026-0163` para «solo call center»); se agrega **un** reclamo de Caja Petrolera para «sin canales». Los valores del DoD (`+59171548278`, `800-10-6060`, `800-12-9992`) son los del spec unitario de la API, no de la maqueta: se registra el desvío.
- **A3 · Suite E2E.** `git mv carril-contacto-aseguradora.spec.ts → carril-insurance-whatsapp.spec.ts` (conserva historial, cumple el nombre del DoD y lane-29) y se extiende; no se borra ningún test.
- **A4 · Maqueta pública (`mockup`).** **Decidido: se porta en esta tarea** (`origin/mockup` tiene hoy los mismos archivos que `dev` en todo lo que se toca: `git diff --name-status origin/dev origin/mockup -- <rutas>` vacío).
- **A5 · «Espacio» sobre un enlace.** Se cumple literal con `(keydown.space)` → `preventDefault()` + `click()` **en el componente**, no en el átomo compartido (alcance).
- **A6 · Umbral de dígitos.** **Decidido: 8 dígitos en front y API.** El DTO pasa de `\d{6,14}` a `\d{7,14}` (rechaza `+` y 7 dígitos; ningún número boliviano se ve afectado: `+591` + 8 = 11) y `whatsappUrl` devuelve `null` con < 8. Se **agrega** el caso de 7 dígitos al spec del front (no existía). Consecuencia a declarar: `hasAnyChannel` (`insurance-contact-channels.ts:75-77`) depende de `whatsapp()`, así que una aseguradora con WhatsApp inválido y sin otro canal muestra «no registró canales» aunque tenga un valor guardado.
- **A7 · Fixtures.** El DoD nombra `core/mock/fixtures/insurance.fixtures.ts`; en este repo las aseguradoras mock viven en el handler (`insurance.handlers.ts:101`). Se sigue el patrón nativo; no se crea el archivo.
- **A8 · Textos de vacío de CA-2.3.** Van con «ej.»: la nota existente «La aseguradora no registró canales de contacto.» (detalle) y «WhatsApp: No informado» / «Call center: No informado» (tarjeta) cuentan como estado vacío honesto; **no** se agregan líneas «No informado» por canal al detalle ni un canal de correo a la tarjeta (exigiría cambiar el lector de coberturas de la API). Supuesto a confirmar en el PR.
- **A9 · Mensaje vacío.** `whatsappUrl(num, '')` devuelve la URL **sin** `?text=` (hoy deja `?text=` vacío). Supuesto.
- **A10 · Criterio de desborde.** Se adopta el del CA (`scrollWidth <= innerWidth`), que es el de la plantilla de la Tarea 1; la suite de la 2.3 usaba `clientWidth`.
- **A11 · Nombre de la aseguradora en el mensaje.** En la maqueta es el nombre corto y contra la API real la razón social; el E2E de maqueta asevera el corto y se declara.

- **Resultado observable:** un operador en la maqueta abre el detalle de `CLM-2026-0177` y el botón «Chatear por WhatsApp» (44 px en móvil, activable con clic, Enter y Espacio) abre en pestaña nueva `https://wa.me/<dígitos>?text=<mensaje codificado sin datos clínicos>`; en `CLM-2026-0163` no hay botón de WhatsApp y sí `btn-call-center-phone` con `tel:800100102`; en el reclamo de Caja Petrolera se lee la nota de ausencia sin ningún enlace; en `/my-account` › «Seguros y tutores» la tarjeta ofrece los mismos botones medibles; nada desborda en 1440 ni en 390.
- **Kill-test:** con `yarn start` (maqueta), `page.getByTestId('btn-call-center-phone')` no existe en ningún detalle, y en 390×844 `getBoundingClientRect()` de `btn-whatsapp-claim` da **40 px** de alto (`size="sm"`: `min-height: 32px` del átomo no baja el `height: 40px` global de `alovida.css:1037-1039`), nunca 44.

## Alcance
- IN: las 14 brechas de arriba, en `telephone.{ts,spec.ts}`, `insurance-contact-channels/**`, `insurance-claim-detail.spec.ts` (usa el testid viejo en `:268`), `patient-coverage-card/**`, `core/mock/handlers/insurance.handlers.ts` (reclamo `CLM-2026-0185` + helper de canales) y `profiles.handlers.ts` (canales de la tarjeta), la suite Playwright renombrada y extendida, `playwright/carril-insurance-antifraud-duplicates.spec.ts:363` y `playwright/patient-coverage-copays.spec.ts:47` (aserciones que cambian con el testid y con M1b), `backbone.dto.{ts,spec.ts}` + README del módulo insurance de la API, PLAN/REPORTE/evidencia, y el porte a `mockup` (A4).
- OUT (se anota, no se toca): `common.contact_points` vs columnas propias (decisión cerrada en 2.3); horario de atención del call center y rastro del clic (T-23, abierto); Alianza Vida y Nacional Seguros sin canales sembrados en Neon (se cargan por el `PUT` cuando alguien administre esas organizaciones); duplicidad de las 9 aseguradoras en Neon; `carril-16-solicitudes-de-seguro.spec.ts` bloqueado por `mockBackend: true` (preexistente); `size="sm"` de otros botones ajenos; el átomo `AppButtonLink`/`Link` (no se les agrega comportamiento); los 2 errores de lint preexistentes en `insurance-claim-detail.spec.ts:198-199`; **correo de la aseguradora en la tarjeta** (exige `support_email` en `declared-coverages-reader.ts:102`, `OwnCoverageDto` y `OwnCoverage`); **la lógica de `wa.me`/`tel:` duplicada fuera de la utilidad** (`insurance-catalog.ts:75-76` arma `wa.me` sin control de `null`, `ficha-de-contacto.ts:38-39` duplica `dialable`, `clinical-record.html:123` pone `tel:` sin limpiar) — deuda registrada; la pestaña «Seguros y tutores» del **editor** de perfil (`patient-profile-edit.html:371-389`, solo lectura, sin canales); los números reales usados como datos de prueba en specs unitarios de la API (`claims-read.service.spec.ts:487`, `insurance-catalog.spec.ts:21`) — nunca se marcan, se anota.

## Protocolo de ramas y hooks
1. **Fase 0 (al salir del modo plan):** en los dos repos `git checkout dev && git pull origin dev`, y `git checkout -b marcelo/feat-insurance-whatsapp-callcenter` (front) / `marcelo/feat-insurance-whatsapp-callcenter-api` (API); primer push con `-u`. **Nunca commitear sobre `dev`**; antes y después de cada push, `git fetch origin dev` + `git log --oneline -3 origin/dev`, y `git branch --show-current` antes de cada corrida de verificación (en esta máquina hay un proceso que mergea ramas a `dev` sin PR y cambia de rama el árbol).
2. Crear `mantra-core-health/docs/trabajo/2026-09-24-whatsapp-callcenter-aseguradora/PLAN.md` (este plan, formato regla 20) **antes del primer Edit de código**; `python .claude/hooks/progress.py --lane 2 --phase <fase> --state <estado> --qa <qa> --message "…"` en cada checkpoint (máximo 3 operaciones materiales sin uno) y `python .claude/hooks/claim.py --lane 2 --level <peldaño> --evidence "…"` por peldaño; al cerrar, `REPORTE.md` (regla 40) y `progress.py --state done` con `REGRESSION_VERIFIED`, o `stop_gate.py` bloquea la sesión.
3. Stagear archivo por archivo (nunca `git add -A`). Commits: `feat(insurance): normalizar enlace y formato E.164 de WhatsApp de aseguradora` · `test(e2e): suite playwright de contacto y canales de aseguradora en desktop y mobile` · `docs(trabajo): plan, reporte y evidencias de certificación de canales de aseguradora`, cada uno con `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
4. Un solo subagente a la vez y nunca en background (hook `resource_guard.py`); Playwright siempre `--workers=1`, sin `&`, `&&` ni `2>&1` en el comando (el hook los rechaza), salida redirigida a archivo.

## H1 — Certificación y cierre del canal WhatsApp / Call Center de aseguradora
**CA:** Dado un operador en la maqueta (y la API real para el contrato de escritura), cuando abre el detalle de un reclamo o su tarjeta de cobertura, entonces se cumplen CA-2.1…CA-2.5 con evidencia literal en `evidencia/`.
**DoD:** todas las microtareas HECHO; regresión API + front en verde; gates seguridad/PHI (regla 90.6), visual con doble revisión (regla 35.1) y `pr-mergeable-gate` (35.2) pegados; `REPORTE.md` escrito.
**Estado:** TODO

### H1.S1 — Backend: mínimo E.164 de 8 dígitos y evidencia (API)
**CA:** Dado `PUT /insurance-carriers/:id/contact-channels`, cuando recibe `+591 71 548278`, `+591-71-548278` o `+591 (71) 548-278`, entonces persiste `+59171548278`; sin `+`, con letras o con **< 8 dígitos** responde 400; la lectura de reclamos y coberturas expone `carrierWhatsappNumber`/`carrierCallCenterPhone` (null cuando la aseguradora no los cargó).
**DoD:** `yarn test src/modules/insurance` verde · `yarn typecheck` 0 · `yarn lint` 0 · contrato regenerado sin diff pendiente.

| ID | Microtarea | CA (binario) | DoD (comando → salida esperada) | Estado |
|---|---|---|---|---|
| H1.S1.M1 | Baseline sobre `dev` limpio (rama recién creada): `backbone.dto.spec.ts`, `claims-read.service.spec.ts`, `declared-coverages-reader.spec.ts` + `yarn typecheck` | Punto de partida registrado | `yarn test src/modules/insurance/dto/backbone.dto.spec.ts src/modules/insurance/services/claims-read.service.spec.ts src/modules/insurance/services/declared-coverages-reader.spec.ts` → passed; salida en `evidencia/api-baseline.txt` | TODO |
| H1.S1.M2 | `backbone.dto.ts:391`: `@Matches(/^\+[1-9]\d{7,14}$/)` (mensaje intacto); JSDoc del campo dice «mínimo 8 dígitos incluido el código de país»; `backbone.dto.spec.ts`: casos nuevos «`+5917154827` (7 dígitos) → error `whatsappNumber` con "E.164"», «`+59171548278` (8 dígitos tras el `+`… 11 en total) → válido», «`+591-71-548278` con guiones → `+59171548278`» (si no está ya) | 7 dígitos → 400; 8 o más → 200 normalizado | `yarn test src/modules/insurance/dto/backbone.dto.spec.ts` → passed (+2/+3 casos) | TODO |
| H1.S1.M3 | Contrato de lectura asegurado: `claims-read.service.spec.ts:487-500` (con canales / null) y `declared-coverages-reader.spec.ts:105-108` (call center sin WhatsApp) ya existen; se corre y se pega; si falta el caso «cobertura con ambos canales», se agrega | Ambos lectores exponen los dos campos en los dos escenarios | los dos specs → passed | TODO |
| H1.S1.M4 | Contrato: el `@ApiProperty` (`backbone.dto.ts:379-384`) no declara `pattern` y `openapi.json` solo trae `type/nullable/example/description`, así que la regex no cambia el contrato; se regenera igual (`docs:openapi:generate` = `yarn build && node tools/openapi/generate-openapi.mjs`, `package.json:104`; + `postman:generate`) y se agrega un ítem **nuevo** «Canales de contacto de la aseguradora» en `src/modules/insurance/README.md` (hoy no menciona E.164 ni `contact-channels`) | CI no fallaría en `git diff --exit-code` sobre lo generado | `git status --short openapi docs/postman` vacío tras regenerar (o el diff pegado y explicado) | TODO |
| H1.S1.M5 | Regresión del módulo | Nada en rojo | `yarn test src/modules/insurance` · `yarn typecheck` · `yarn lint` → salidas literales en `evidencia/` | TODO |

### H1.S2 — Frontend: utilidad, componente del detalle y tarjeta de cobertura
**CA:** Dado un reclamo o una cobertura con canales, cuando se renderiza, entonces el WhatsApp (`btn-whatsapp-claim` / `btn-whatsapp-coverage`) abre `wa.me` con el mensaje acordado (A1) sin campos clínicos, en pestaña nueva, con clic/Enter/Espacio y ≥ 44 px en móvil; sin WhatsApp no existe el botón y sí `btn-call-center-phone` (`tel:` sin separadores); sin canales, la nota de ausencia; letras o < 8 dígitos → sin enlace.
**DoD:** specs de `telephone`, `insurance-contact-channels` y `patient-coverage-card` verdes · `yarn typecheck` 0 · `yarn lint` sin problemas nuevos · `check-css-tokens` / `check-architecture` / `check-contrast` PASS.

| ID | Microtarea | CA (binario) | DoD | Estado |
|---|---|---|---|---|
| H1.S2.M1 | Baseline front: los 3 specs + `yarn typecheck` sobre `dev` limpio | Punto de partida registrado | `yarn test --watch=false --include="src/app/shared/utils/telephone/**/*.spec.ts" --include="src/app/features/insurance/insurance-claim-detail/insurance-contact-channels/**/*.spec.ts" --include="src/app/shared/components/molecules/patient-coverage-card/**/*.spec.ts"` → passed; `evidencia/front-baseline.txt` | TODO |
| H1.S2.M2 | `telephone.ts`: `whatsappUrl` devuelve `null` si `raw` contiene letras (`/[a-z]/i`) o deja < 8 dígitos (coherente con la API tras S1.M2); con `text` vacío arma la URL sin `?text=`; JSDoc actualizado (la utilidad ahora sí descarta, cita CA-2.4); `telephone.spec.ts` cubre letras, 7 vs 8 dígitos, espacios, guiones, paréntesis, prefijo no boliviano (`+54 11 4321 5678`), mensaje vacío; el caso «`'123'` → null» sigue y se agrega el de 7 dígitos con comentario que cita CA-2.4 | `whatsappUrl('+591abc78', 'x') === null`; `whatsappUrl('+5917154827', 'x') === null`; `whatsappUrl('+59171548278', '')` sin `?text=` | `yarn test --watch=false --include="src/app/shared/utils/telephone/**/*.spec.ts"` → passed | TODO |
| H1.S2.M3 | `insurance-contact-channels` (el texto del mensaje **no se toca**, A1): testid `btn-callcenter-claim` → `btn-call-center-phone` en `insurance-contact-channels.html:29`, `insurance-contact-channels.spec.ts:84,98,115`, **`insurance-claim-detail.spec.ts:268`**, `carril-contacto-aseguradora.spec.ts:65,85` (→ nueva suite) y `carril-insurance-antifraud-duplicates.spec.ts:363`; WhatsApp y call center a `size="md"` (`button-link.ts:58-65` acepta `size` y emite `btn--md` → `min-height: 44px` < 780, 40 ≥ 780; el host `(click)` de `AppButtonLink` no hace `preventDefault` ni `stopPropagation`, `:53,75-77`); `(keydown.space)="onSpace($event)"` en el ancla de WhatsApp → `preventDefault()` + `(target as HTMLAnchorElement).click()` (Enter ya es nativo); `callCenterHref` con guard `phone && dialable(phone)` (brecha 12); spec: testid nuevo, PHI-guard (cabecera con campos extra tipo `diagnosisText`/`medicationText`/`clinicalNotes` inyectados por spread → **no** aparecen en el texto decodificado), Espacio: `vi.spyOn(ancla, 'click').mockImplementation(() => {})` (jsdom no navega), despachar `new KeyboardEvent('keydown', { key: ' ', cancelable: true })`, aseverar `click` llamado una vez y `defaultPrevented === true`; `size` md en los dos botones; `N/A` → sin enlace de call center | Los casos del spec pasan y `grep -rn btn-callcenter-claim src playwright docs` = 0 | `yarn test --watch=false --include="src/app/features/insurance/insurance-claim-detail/**/*.spec.ts"` → passed; grep = 0 | TODO |
| H1.S2.M4 | `patient-coverage-card` (mensaje y `app-link` intactos: la tarjeta ya pasó la doble revisión de la Tarea 1): `data-testid="btn-whatsapp-coverage"` en el enlace de WhatsApp (con `[external]="true"` que ya pone `target/rel`) y `data-testid="btn-call-center-coverage"` en el de call center; `(keydown.space)` en el de WhatsApp; `.coverage__contacts a` gana `min-width` igual al `min-height` que ya tiene (44 px, mismo valor/token que la regla existente `:62-66`); spec actualizado: los `querySelector('a')` pasan a los testids, `target="_blank"`/`rel` exactos, Espacio dispara `click`, sin WhatsApp el testid no existe | La tarjeta expone los dos enlaces medibles y el vacío honesto | spec de la tarjeta → passed | TODO |
| H1.S2.M5 | Docs que citen el testid o el texto viejo (`docs/routes/*insurance*`, `docs/components/*`, README del feature) | Ninguna referencia a `btn-callcenter-claim` | `grep -rn "btn-callcenter-claim" docs src playwright` = 0 | TODO |
| H1.S2.M6 | Gates estáticos del front (`frontend-production-gate`) | Nada nuevo en rojo | `yarn typecheck` → 0 · `yarn lint` (rojos preexistentes listados con causa) · `yarn build` → exit 0 · `node scripts/check-css-tokens.mjs` · `node scripts/check-architecture.mjs` · `node scripts/check-contrast.mjs` → PASS, salidas en `evidencia/` | TODO |

### H1.S3 — Maqueta y E2E Playwright (`playwright/carril-insurance-whatsapp.spec.ts`)
**CA:** Dado `yarn start` (maqueta) en `localhost:4200`, cuando corre la suite con `--workers=1`, entonces pasan CA-2.1 (href, texto, `target/rel`, pestaña nueva real, Enter y Espacio), CA-2.2 (Vitalicia), CA-2.3 (Caja Petrolera), CA-2.5 (44×44 en 390×844, sin desborde en 1440 y 390) en el detalle y en `/my-account` › «Seguros y tutores», con capturas revisadas dos veces.
**DoD:** `yarn pw playwright/carril-insurance-whatsapp.spec.ts --workers=1 -g "maqueta"` → N/N passed (el `baseURL` ya cae a `http://localhost:4200`, `playwright.config.ts:43`; en PowerShell la variable va aparte: `$env:E2E_BASE_URL='http://localhost:4200'`) · bloque `-g "API"` 3/3 con la API en `E2E_API_URL` o `BLOCKED` documentado · capturas en `evidencia/` · `evidencia/doble-revision.md` con nota y puntuación por pantalla.

| ID | Microtarea | CA (binario) | DoD | Estado |
|---|---|---|---|---|
| H1.S3.M1 | Maqueta: agregar la tupla `['CLM-2026-0185', 4, '320.00', null, -3, 'SUBMITTED', 'Enviada', false, [['Consulta general', '320.00', null, 'PENDING']]]` al final de `solicitudes` (`insurance.handlers.ts:453-548`; forma `[claimIdentifier, carrierIndex, billed, approved, dias, code, display, disputa, lineas]`) **y extender el literal paralelo de pacientes de `:556` de 6 a 7 índices** (`[0, 5, 2, 0, 8, 0, 1]` — `1` = `p-mamani`, `personas.ts:336`, sin aseguradora ni reclamos, para no alterar el certificado de portabilidad «14 reclamos» ni `solicitudDeLaCita`); comentario «escenario sin canales (CA-2.3)». Sin el 7.º índice `PACIENTES[undefined]` lanza `TypeError` al evaluar el módulo y cae el simulador entero. Efecto lateral a declarar: `todas[semilla % todas.length]` (`:597`) cambia qué reclamo se «presta» a las citas | `GET /insurance-claims` simulado devuelve 7 ítems y `CLM-2026-0185` trae los tres canales en `null`; el módulo carga sin `TypeError` | `yarn test --watch=false --include="src/app/core/mock/handlers/insurance.handlers.spec.ts" --include="src/app/core/mock/handlers/insurance-portability.handlers.spec.ts" --include="src/app/core/mock/handlers/scheduling.handlers.spec.ts" --include="src/app/core/mock/mock-backend.spec.ts"` → passed | TODO |
| H1.S3.M1b | Maqueta de `/my-account` sin números reales (brecha 13): exportar de `insurance.handlers.ts` `contactChannelsOfCarrier(name: string): { whatsapp: string \| null; callCenter: string \| null }` que busca por `name` en `ASEGURADORAS` y devuelve nulls si no lo encuentra (los 107 pacientes generados usan `fk.ASEGURADORAS`); `profiles.handlers.ts:315-316,324,329` lo consumen conservando la asimetría actual (vigente ambos, vencida solo WA, futura solo CC). Sin ciclo de imports: `scheduling.handlers.ts:16` e `insurance-portability.handlers.ts:10` ya importan de `./insurance.handlers`. Ajustar `patient-coverage-copays.spec.ts:47` (`tel:800106060` → `tel:800100101`). El `+591 70011223` de `profiles.handlers.ts:336` es el teléfono de un tutor, no un canal: se anota, no se toca | Para `paciente` la tarjeta expone `wa.me/59170000101` y `tel:800100101`; ningún número real de una aseguradora sembrada queda en la maqueta | `yarn test --watch=false --include="src/app/core/mock/**/*.spec.ts"` → passed; `grep -rn "800-10-6060" src/app/core/mock` = 0 | TODO |
| H1.S3.M2 | `git mv playwright/carril-contacto-aseguradora.spec.ts playwright/carril-insurance-whatsapp.spec.ts` y reescribir el bloque de maqueta con la plantilla de la Tarea 1: sin `estable()` (`networkidle` prohibido), web-first assertions, copiar `screenshotWithoutOverflow` y `esRuidoDelServidorDeDesarrollo` (son privados en `carril-insurance-portability.spec.ts:65-74`; precedente de copia: `patient-coverage-copays.spec.ts:6-10`), viewports explícitos por test (el project fuerza 1280×720), entrada por el listado (`tabla-fila` → `claim-link`), capturas con `info.outputPath`, `describe` de maqueta con «maqueta» en el título y el de API con «API» (para `-g`); identificadores nuevos en inglés; el bloque API·Neon (`:123-210`) se conserva tal cual (no usa el testid) | Historial conservado; suite compila | `yarn typecheck` → 0 (cubre `playwright/tsconfig.json`) | TODO |
| H1.S3.M3 | E2E CA-2.1 en el detalle (`CLM-2026-0177`, Alianza): `href` empieza por `https://wa.me/59170000103?text=`, texto decodificado contiene `CLM-2026-0177` y «Alianza Seguros» y **no** contiene «ECG», «Cláusula» ni «Control cardiológico» (las líneas clínicas de ese reclamo: PHI-guard en runtime, CA-2.4), `target/rel`. Popup real (sin precedente en el arnés; `context.waitForEvent('page')` es el evento correcto porque `rel=noopener` desliga al opener): en `beforeEach` `context.route('**/wa.me/**', r => r.fulfill({ status: 200, contentType: 'text/html', body: '' }))`; por cada activación `const popupPromise = context.waitForEvent('page'); await accion(); const popup = await popupPromise; await expect(popup).toHaveURL(/wa\.me\/59170000103\?text=/); await popup.close();` con `accion` = `click()`, `focus()` + `keyboard.press('Enter')`, `focus()` + `keyboard.press('Space')` (Playwright emite `key: ' '`, que Angular normaliza a `keydown.space`) | CA-2.1 observado con las tres activaciones | spec → passed | TODO |
| H1.S3.M4 | E2E CA-2.2 (`CLM-2026-0163`, Vitalicia): `btn-whatsapp-claim` `toHaveCount(0)`, `btn-call-center-phone` visible con `href="tel:800100102"` | CA-2.2 observado | spec → passed | TODO |
| H1.S3.M5 | E2E CA-2.3 (`CLM-2026-0185`, Caja Petrolera): `insurance-contact-absent` visible con «no registró canales» y `insurance-contact-channels` con `locator('a')` `toHaveCount(0)` | CA-2.3 observado | spec → passed | TODO |
| H1.S3.M6 | E2E CA-2.5 por viewport (contexto propio por ancho: `browser.newContext({ viewport })`; 390×844, 768×1024 y 1440×900 — los dos del CA más la tablet que exige la regla 80.6; 1024×768 y 1920×1080 del `visual-quality-gate` se omiten con justificación escrita: microcambio en una barra `flex-wrap` sin impacto de layout de página): `getBoundingClientRect()` de `btn-whatsapp-claim` y `btn-call-center-phone` ≥ 44×44 en 390 (≥ 40 en 1440); `document.documentElement.scrollWidth <= window.innerWidth` en todos; capturas `contacto-detalle-<ancho>.png` | Sin scroll horizontal; objetivos ≥ 44 en móvil | spec → passed ×3 viewports; PNG en `evidencia/` | TODO |
| H1.S3.M7 | E2E tarjeta: `entrarAlSimulador(page, 'paciente', '')` (`playwright/support/simulador.ts:30`, firma `(page, usuario, base)`) → `page.goto('/my-account')` → `getByRole('tab', { name: 'Seguros y tutores' })` → `btn-whatsapp-coverage` (`.first()`) con `href` `wa.me/59170000101?text=` (tras M1b), `target="_blank"` y `rel="noopener noreferrer"`, ≥ 44×44 en 390, clic → popup y Espacio → popup con el mismo protocolo de M3 (`popupPromise` antes, `toHaveURL`, `close()`); `btn-call-center-coverage` `tel:800100101`; `scrollWidth <= innerWidth`; captura `contacto-tarjeta-<ancho>.png` | CA-2.1/2.5 observados en la segunda superficie | spec → passed ×2 (390 y 1440) | TODO |
| H1.S3.M8 | Corrida de la suite en dos partes: `yarn pw playwright/carril-insurance-whatsapp.spec.ts --workers=1 -g "maqueta"` → N/N (`evidencia/playwright-whatsapp-maqueta.txt`) y `… -g "API"` → 3/3 con la API levantada y `$env:E2E_API_URL` (por defecto `:3005`, `support/actores.ts:51`), o `BLOCKED` con la salida literal (bloque preexistente). Doble revisión (regla 35.1): pasada 1 verificación con la puntuación de 10 criterios del `visual-quality-gate` (PASS ≥ 92, ningún HIGH), pasada 2 adversarial con el agente `visual-reviewer` del repo (10 preguntas de `critical-double-review` §3), nota por pantalla | Ninguna pantalla RECHAZADA | `evidencia/playwright-whatsapp-*.txt` · `evidencia/doble-revision.md` | TODO |
| H1.S3.M9 | Regresión E2E del módulo, `--workers=1`, en serie: `carril-adjudicacion-clausulas.spec.ts` (2.2), `carril-insurance-antifraud-duplicates.spec.ts` (usa el testid renombrado), `patient-coverage-copays.spec.ts` (tarjeta, números de M1b) y `carril-insurance-portability.spec.ts` (Tarea 1, comparte `/my-account`) | Nada en rojo que no estuviera antes | salidas literales en `evidencia/` | TODO |

### H1.S4 — Regresión, revisión independiente, `mockup`, PRs y reporte
| ID | Microtarea | CA (binario) | DoD | Estado |
|---|---|---|---|---|
| H1.S4.M1 | Regresión front completa: `yarn test --watch=false` (si esbuild/pool falla, un reintento documentado, nunca `skip`) + `yarn build` + consumidores de la molécula compartida (`patient-coverage-card` solo se monta en `my-profile.html:424-439`: se cubre con S3.M7) | Nada nuevo en rojo (los preexistentes, listados con causa) | salida literal en `evidencia/front-suite.txt` y `evidencia/front-build.txt` | TODO |
| H1.S4.M2 | Revisión independiente (NO_SELF_APPROVAL): agente `frontend-reviewer` del repo sobre el diff del front y `/code-review` sobre el de la API; corregir todo `BLOCKER`/`CRITICAL`/`HIGH` y re-correr los specs afectados | Ningún hallazgo ≥ HIGH abierto | informe del revisor en `evidencia/revision-independiente.md` | TODO |
| H1.S4.M3 | Porte a `mockup` (decidido): `git checkout mockup && git pull origin mockup && git checkout -b marcelo/feat-insurance-whatsapp-callcenter-mockup`; cherry-pick de los commits del front (hoy diff vacío entre ramas en lo tocado: no se esperan conflictos; si aparecen, se resuelven conservando lo visual de `mockup`); specs de S2 + suite S3 en esa rama (`git branch --show-current` antes); PR → `mockup` con el mismo cuerpo y reviewers | La demo pública muestra lo mismo que `dev` | salidas en `evidencia/mockup-*.txt`; `gh pr view` mergeable | TODO |
| H1.S4.M4 | Push de las tres ramas (`-u origin`); PR API primero (contrato: mínimo 8 dígitos), luego PR front → `dev` referenciándolo, luego PR front → `mockup` (cuerpo: fuente `REGISTRO :492` = 6.2·5 = 2.3, CA-2.1…2.5 con evidencia, ambigüedades A1–A7 con lo decidido, «No cubierto», atribución final); `gh pr create --base <dev|mockup> --reviewer jsaldias39,PabloArauzCaballero`; `gh pr view --json number,url,isDraft,mergeable,mergeStateStatus,reviewDecision` + `gh pr checks`; verificar que `origin/dev` no absorbió la rama | PRs `MERGEABLE` y `CLEAN`/`BLOCKED` solo por revisión humana | salidas literales de `gh` en `evidencia/` | TODO |
| H1.S4.M5 | `REPORTE.md` (avance en la 1.ª línea, Completado/A medias/Pendiente, Evidencia, No cubierto, Desvíos, Riesgos, Decisiones/ambigüedades A1–A11, gate de seguridad/PHI 90.6), `<workspace>/docs/tareas/subtarea-2.5-whatsapp-callcenter-aseguradora/walkthrough.md` apuntando al reporte (mismo formato que `subtarea-2.3-canales-contacto-aseguradora/walkthrough.md`, que vive en la raíz del workspace, no en los repos), `claim.py --level` real, `progress.py --state done`, memoria del proyecto | Alguien que no vio la sesión puede retomar | archivos en disco; `stop_gate` no bloquea | TODO |

## Orden de ejecución
S1.M1 → S2.M1 (baselines, incluido el kill-test de 40 px) → S1.M2→M3→M4→M5 (API) → S2.M2→M3→M4→M5→M6 → S3.M1→M1b→M2 → S3.M3…M7 (escribir E2E) → S3.M8 (corrida + capturas + doble revisión) → S3.M9 → S4.M1→M2 → S4.M3 (mockup) → S4.M4 → S4.M5. **26 microtareas** (S1 5 · S2 6 · S3 10 · S4 5): el avance del REPORTE sale de `HECHO / 26`. Una microtarea `EN CURSO` a la vez; el `PLAN.md` de `docs/trabajo/` se actualiza al abrir y cerrar cada una.

## Verificación de punta a punta
- Skill **verify** del proyecto: cada DoD con comando y salida literal en `evidencia/`; nada se declara por lectura. Peldaños con `claim.py` (`DISCOVERED` al cerrar baselines, `WRITTEN`, `RUNS`, `TESTED`, `VERIFIED` tras S3.M8, `REGRESSION_VERIFIED` tras S4).
- Reglas del arnés: front `yarn test --watch=false --include=… --include=…` (varios `--include` tienen precedente en `docs/trabajo/2026-09-20-ender-contratos-panel/REPORTE.md:22`; Vitest 4, Yarn PnP, nunca npm); API `yarn test <ruta>` (nunca `npx jest`); Playwright `--workers=1`, sin `&`/`&&`/`2>&1`, salida a archivo, nunca `networkidle`, `yarn start` escuchando antes (no hay `webServer`; `baseURL` cae a `localhost:4200`), en PowerShell la variable de entorno va en una línea aparte (`$env:E2E_BASE_URL='…'`), pantalla de acceso en `/auth` con `login-identifier`/`login-password`/`login-submit`, un `browser.newContext({ viewport })` por ancho, `git branch --show-current` antes de cada corrida. **Ojo:** el hook `resource_guard.py:81` matchea `playwright test` y `yarn pw` incluso dentro de cadenas de un `echo`/`grep`: no escribirlos en otros comandos.
- Evidencia: se guarda en `docs/trabajo/2026-09-24-whatsapp-callcenter-aseguradora/evidencia/` (regla 20 y precedente de la Tarea 1); el `visual-quality-gate` del front nombra `docs/frontend/evidence/<microtarea>/` — se deja un `README.md` de una línea ahí apuntando a la carpeta de trabajo, para no duplicar capturas.
- Seguridad/PHI (regla 90.6): amenaza = datos clínicos o identificatorios del paciente en una URL hacia un tercero (Meta) → control = mensaje construido solo desde `claimIdentifier` + `carrierName` (A1) y test PHI-guard (S2.M3) → evidencia = spec + `decodeURIComponent(href)` en el E2E → riesgo residual = el código de reclamo viaja igual (identificador interno, no clínico), declarado.
- Visual (regla 35.1): capturas por ancho en las dos superficies, dos pasadas, nota por pantalla.

## Riesgos y bloqueos previstos
| Riesgo | Impacto | Mitigación |
|---|---|---|
| El popup de `wa.me` intenta salir a Internet o llega como `about:blank` | Test lento o flaky | `context.route('**/wa.me/**')` con `fulfill` en `beforeEach`; `waitForEvent('page')` creado **antes** de la acción; `expect(popup).toHaveURL(...)` (web-first), nunca `popup.url()` inmediato; `popup.close()` en cada activación |
| El reclamo nuevo corre el índice `semilla % todas.length` de las citas | Otra cita «presta» otro reclamo en la maqueta | `scheduling.handlers.spec.ts` no asevera `insuranceClaim`; se corre igual en S3.M1 y se declara |
| `Space` sobre `<a>` provoca scroll en vez de clic | CA-2.1 rojo en teclado | `(keydown.space)` con `preventDefault()` local al componente; spec unitario + E2E |
| Cambiar `sm` → `md` mueve el layout de la barra de canales | Captura escritorio distinta a la de 2.3 | `flex-wrap` ya apila; se verifica en 390/1440 y se documenta en la doble revisión |
| No hay reclamo mock con la aseguradora sin canales | CA-2.3 sin E2E | S3.M1 lo agrega con id determinista; el handler ya tiene a Caja Petrolera |
| `mockup` diverge de `dev` en los componentes tocados | Cherry-pick con conflictos | Resolver conservando lo visual de `mockup`; barrer borrados con `git diff --name-only --diff-filter=D` |
| `carrierName` corto en maqueta vs razón social en la API | El texto del mensaje difiere por entorno | El E2E de maqueta asevera el nombre corto; el spec unitario cubre el mapeo; se declara (A11) |
| Cambiar `800-10-6060` en la maqueta rompe aserciones ajenas | Rojos en specs de handlers/E2E | M1b hace `grep` previo y actualiza cada aserción en el mismo commit |
| `min-width: 44px` en el enlace de la tarjeta cambia el layout de `.coverage__contacts` | Captura distinta a la de la Tarea 1 | Verificar en 390/1440 y documentar en la doble revisión |
| Proceso local que mergea a `dev` y cambia de rama el árbol | Evidencia inválida / push a `dev` sin PR | `git branch --show-current` antes de cada corrida; `git log origin/dev` antes y después de cada push |
| Pool de Vitest / esbuild segfault | Falsos rojos | Un reintento documentado; nunca `skip` |
