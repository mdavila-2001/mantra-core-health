# Plan — Portabilidad de póliza e historial de siniestralidad a 1 clic (certificación E2E y cierre)

- Fecha: 2026-09-24 · Repos afectados: `mantra-core-health-api` (rama `marcelo/feat-insurance-portability-hardening-api`) y `mantra-core-health` (rama `marcelo/feat-patient-insurance-portability-e2e`), ambas desde `origin/dev`. Modelo y bóveda: **sin cambios** (la persistencia ya existe).
- Predecesor: subtarea 3.3, mergeada en `dev` (API PR #433 · front PRs #525 y #546, 19–20/09) + copy «honesto» del PR #609 (solo en `mockup`, commit `c1578610`).
- Fuente del requisito: `REGISTRO DE PROCESOS POR MODULO.md:499` = bóveda 6.3 · ítem 5 = `docs/tareas/subtarea-3.3-portabilidad-siniestralidad/`. Las tres numeraciones se citan juntas; el PR se titula «Subtarea 3.3» como su predecesor.

## Contexto (qué descubrió la FASE 0)

**La feature ya está en `dev`, entera y con persistencia real.** Los 4 endpoints existen (`insurance-portability.controller.ts`, `insurance-portability-public.controller.ts`); el certificado se guarda como archivo PHI en `common.files` + `health_data.health_export_jobs`/`health_export_manifests` (el `content_hash` ES el sello) + `dsar_requests` + `data_access_log` + `audit_log`, todo en una transacción (`insurance-portability.service.ts:251-320`). El 403 ya escribe `INSURANCE_PORTABILITY_DENIED` en `audit.audit_log` (`:427-451`) vía `ProfileOwnershipService.assertOwnsPatientProfile`. El PDF (pdfkit + qrcode, helpers de recetas) imprime resumen actuarial, sello y QR a `${WEB_APP_BASE_URL}/verify/portability/<hash>`. Las 3 ramas `marcelo/*` de portabilidad están fusionadas con 0 commits propios.

**Baseline medido (dev limpio, API `7541797c`, front `dc9444e8`):** API spec 7/7 · front card+diálogo 17/17, verify 8/8, cliente 4/4, handlers 9/9 (un segfault de esbuild en el 1.er intento, verde al reintentar) · `yarn typecheck` front exit 0.

**Brechas vigentes contra CA-01..04 (verificadas en código, ninguna refutada):**
1. CA-01: el diálogo preselecciona `PDF` → confirmar NO descarga los dos archivos; BUNDLE no tiene E2E.
2. CA-01: el JSON no trae «histórico de atenciones» (no consulta `clinical.encounters`); el copy lo promete.
3. CA-04: la API pública rechaza el hash en MAYÚSCULAS con 400 (`ParseShaHashPipe` `/^[0-9a-f]{64}$/`); solo el front normaliza.
4. CA-04: el verify devuelve `generatedAt = job.requestedAt` (otro `new Date()` en el repositorio) ≠ el `generatedAt` sellado en JSON/PDF.
5. No existe `insurance-portability-pdf.service.spec.ts`; el spec del servicio no cubre sin-coberturas, ownership en descargas ni el payload VALID.
6. CA-03 solo con mocks: ningún int-spec, ningún 403 HTTP ni fila real de `audit_log` observados.
7. Escape/click de fondo cierran y desmontan el diálogo en pleno `loading` (`(dismissAttempt)="close()"` anula `dismissible=false`).
8. «Copiar hash» y las descargas del estado `ready` miden 32 px en móvil (`size="sm"`); el E2E no los mide.
9. Identificadores en castellano (lane-29) en archivos nuevos de la subtarea: PDF service (`armarCertificado`, `dibujar`, `PapelDePortabilidad`…), clases BEM, `data-testid="portability-hash-copiado"`, `id="portability-hash-rotulo"`, `formatos`, `enLaDireccion`, funciones del handler mock y variables del spec Playwright. `btn-generate-portability-download` está duplicado en dos botones.
10. El copy honesto (sin «oficial», «firma digital», «expediente completo») vive solo en `mockup`.

**Desvíos del prompt (el repo desmiente):** el spec vive en `services/`, no en `src/modules/insurance/tests/` (esa carpeta no existe); no hay spec del PDF; `wire.ts` no es un registro de clientes (el cliente es `providedIn: 'root'`); el E2E existente es **solo maqueta** (`mockBackend: true` en dev desde 05/09); `docs/trabajo/` no existe en la raíz del workspace.

**Decisiones tomadas con el usuario (2026-09-24):** (1) BUNDLE por defecto; (2) agregar sección `encounters` mínima; (3) cherry-pick de `c1578610` a dev; (4) CA-03 se verifica contra Neon **sin reset** (el arnés con `reset` hace `TRUNCATE`; Docker está apagado).

- Resultado observable: un paciente titular en `/my-account` › «Seguros y tutores» confirma el diálogo y se bajan JSON + PDF (con QR y sello); un auditor abre `/verify/portability/<HASH en cualquier caso>` sin sesión y ve metadatos sin PHI; un usuario que manda el `patientProfileId` de otro recibe 403 y deja fila en `audit.audit_log`.
- Kill-test: con `yarn start` (maqueta), el spec Playwright no tiene ningún test que espere DOS descargas; y contra la API, `GET /public/portability/verify/<HASH EN MAYÚSCULAS>` responde 400.

## Alcance
- IN: los 10 puntos de arriba, en los archivos de portabilidad de API y front + el mock + el spec Playwright + docs de ruta/README + PLAN/REPORTE/evidencia.
- OUT (se anota, no se toca): bypass de `SUPERADMIN`/`SECURITY_ADMIN` (convención transversal, `profile-ownership.service.ts:147`); consentimiento/`data_use_agreements` (§24 punto 3, T-27 c); `WEB_APP_BASE_URL` en Coolify (ítem de despliegue, va al handoff); botón de cerrar del organismo `content-dialog` (32 px, compartido); helpers importados de `clinical/`/`common/money` (`dibujarQr`, `sumarDecimales`…); commit huérfano `b3976b72` de la rama codex (docs H1–H5); nombres de archivo descargados `portabilidad-<id>.*` (prosa visible, no identificador — ambigüedad registrada).
- Ambigüedades registradas: `recordCount` sigue contando reclamos (no atenciones) — se documenta en el DTO; `encounters` excluye `reason_text` (texto clínico libre) por minimización PHI; el título del PDF pasa a «Certificado de portabilidad de siniestralidad» (sin «oficial», coherente con #609) aunque CA-01 diga «PDF oficial» — confirmar con Justin/Pablo en el PR.

## Protocolo de ramas y hooks
1. Front: `git checkout dev && git pull origin dev && git checkout -b marcelo/feat-patient-insurance-portability-e2e`. API: ídem con `marcelo/feat-insurance-portability-hardening-api`. **Nunca commitear sobre `dev`**; antes y después de cada push, `git fetch origin dev` y `git log --oneline -3 origin/dev` (en esta máquina hay un proceso que mergea ramas a dev sin PR y hasta cambia de rama el árbol: verificar `git branch --show-current` antes de cada corrida).
2. Al salir del modo plan: crear `mantra-core-health/docs/trabajo/2026-09-24-portabilidad-aseguradora/PLAN.md` (este plan, formato regla 20) **antes del primer Edit de código**; registrar `python .claude/hooks/progress.py` en cada checkpoint y `claim.py` por peldaño; al cerrar, `REPORTE.md` (regla 40) o el `stop_gate` bloquea.
3. Stagear archivo por archivo (nunca `git add -A`). Commits `feat(insurance-portability): …` / `test(e2e): …` con `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.

## H1 — Certificación E2E y cierre productivo de la portabilidad a 1 clic
**CA:** Dado un titular en la maqueta y en la API real, cuando exporta, verifica y un tercero intenta exportar su historial, entonces se cumplen CA-01..CA-04 con evidencia literal en `evidencia/`.
**DoD:** todas las microtareas HECHO; regresión API+front en verde; gates seguridad/PHI, visual (doble revisión) y `pr-mergeable-gate` pegados; REPORTE.md escrito.
**Estado:** TODO

### H1.S1 — Capa Backend: endurecimiento y evidencia (API)
**CA:** Dado `POST /insurance/portability/export`, `GET …/certificates/:id/{pdf,json}` y `GET /public/portability/verify/:hash`, cuando se ejercitan con titular, ajeno, sin coberturas y hash en mayúsculas, entonces responden 201/403/200 con metadatos coherentes, sin PHI en el verify y con fila en `audit_log`.
**DoD:** `yarn test src/modules/insurance` verde · `yarn typecheck` 0 · `yarn lint` 0 · int-spec verde contra Neon · OpenAPI/Postman regenerados sin diff pendiente.

| ID | Microtarea | CA (binario) | DoD (comando → salida esperada) | Estado |
|---|---|---|---|---|
| H1.S1.M1 | Verify público insensible a mayúsculas: `ParseShaHashPipe` acepta `[0-9a-fA-F]{64}` y devuelve `value.toLowerCase()` (`insurance-portability-public.controller.ts:23-40`); nuevo `insurance-portability-public.controller.spec.ts` (pipe: mayúsculas/mixto → mismo hash en minúsculas; 63 chars o no-hex → `BadRequestException`) | Hash en mayúsculas → 200 con los mismos metadatos que en minúsculas | `yarn test src/modules/insurance/controllers/insurance-portability-public.controller.spec.ts` → passed | TODO |
| H1.S1.M2 | `generatedAt` único: `DataReleaseRepository.createExportJob` acepta `requestedAt?` (`data-release.repository.ts:195-263`), el servicio pasa `generatedAt` (`:252`); verify devuelve ese instante | `verify().generatedAt === report.generatedAt` | spec del servicio: caso nuevo «verify devuelve el instante sellado» → passed | TODO |
| H1.S1.M3 | Sección `encounters`: `InsurancePortabilityRepository.encountersOfPatient(em, id)` sobre `clinical.encounters` (columnas reales de `encounters.entity.ts`: `start_at`, `end_at`, `class/type/status_concept_id`, `tenant_id`, `branch_id`; **sin** `reason_text`) ⟕ `directory.tenants` ⟕ `directory.branches` (verificar el nombre de la columna de nombre en sus entidades antes de escribir), `ORDER BY start_at, id`; `PortabilityEncounterDto {encounterId, startAt, endAt, class, type, status, organizationName, branchName}` (códigos vía `conceptMap`); `report.encounters`; `SCHEMA_VERSION = 'alovida.insurance-portability/2'`; PDF: sección «Atenciones» (fecha · tipo · organización · estado) y subtítulo coherente | El JSON sellado trae `encounters[]` y el PDF una sección «Atenciones»; sin encuentros, `[]` y «Sin atenciones registradas.» | spec del servicio (mapeo + vacío) y spec del PDF → passed; `yarn typecheck` → 0 | TODO |
| H1.S1.M4 | Nuevo `services/insurance-portability-pdf.service.spec.ts` (co-locado, como el resto): `armarCertificado`/`buildCertificate` → líneas exactas del resumen actuarial, pie «SHA-256 <hash> · verificar en <qrUrl>», «El titular no declaró ninguna póliza.», destacados > 2000, sección atenciones; `render()` → Buffer que empieza por `%PDF-` y contiene `sello:<hash>` | El texto del certificado y el binario están asegurados | `yarn test src/modules/insurance/services/insurance-portability-pdf.service.spec.ts` → passed | TODO |
| H1.S1.M5 | Caso «sin coberturas» explícito en `insurance-portability.service.spec.ts`: repos vacíos → resultado con `policiesCount 0`, `recordCount 0`, `summary.currencyCode null`, `estimatedLossRatioPercent null`, montos `'0.00'`, y se llaman `createExportJob`/`createExportManifest`/`dsarRepo.create`/`auditTrail.record` (la constancia) | Sin coberturas exporta igual, sin lanzar | spec → passed | TODO |
| H1.S1.M6 | Ownership en descargas: spec de `renderPdf`/`downloadJson` con actor ajeno → `ForbiddenException` + `INSURANCE_PORTABILITY_DENIED` y `downloadForAuthorizedContext` **no** llamado; payload VALID de `verify` aseverado campo a campo (7 campos, ninguno PHI) | Descargar el certificado de otro → 403 sin bytes | spec → passed | TODO |
| H1.S1.M7 | `test/integration/insurance-portability.int-spec.ts` (harness `bootstrapTestApp({reset: false})`, dos cuentas sintéticas por HTTP como `patient-own-profile.int-spec.ts:419`): (a) POST export con `patientProfileId` ajeno → **403** HTTP, cuerpo sin nombre/documento del otro; (b) `SELECT action, entity_id, outcome_concept_id FROM audit.audit_log WHERE action='INSURANCE_PORTABILITY_DENIED' AND entity_id=<ajeno>` → 1 fila; (c) titular: 201 → GET json → `sha256(bytes) === manifestHash` → GET verify con el hash en MAYÚSCULAS → 200 y `generatedAt` igual al del JSON | CA-03 observado en runtime, no mockeado | `yarn test:integration --testPathPatterns=insurance-portability` → passed (+ salida literal del SELECT en `evidencia/`) | TODO |
| H1.S1.M8 | lane-29 en los archivos propios de la subtarea: `armarCertificado→buildCertificate`, `dibujar→draw`, `PapelDePortabilidad→PortabilitySheet`, `TablaFila→TableRow`, campos `titulo/subtitulo/afiliado/polizas/…→title/subtitle/member/policies/…`, `seccion→section`, `tabla→table`, `anchoMarca→markWidth`, `ladoQr→qrSide`, `documento→nationalIdentifier`, `em0→lookupEm`; título del PDF sin «oficial»; **no** se tocan `dibujarQr`, `dibujarMarcaAlovida`, `altoDeMarca`, `sumarDecimales` | `grep -nE 'armarCertificado|dibujar\(|PapelDePortabilidad|TablaFila|anchoMarca|ladoQr' src/modules/insurance` = 0 | `yarn typecheck` → 0; `yarn test src/modules/insurance` → passed | TODO |
| H1.S1.M9 | Contrato: regenerar OpenAPI/Postman con los scripts del repo (los que corre `docs.yml`: generación del contrato + `postman:generate`) y sección «Portabilidad del titular» en `src/modules/insurance/README.md` (4 rutas, persistencia en `health_data`, auditoría, `WEB_APP_BASE_URL`) | CI no falla en `git diff --exit-code` | `git status --short openapi docs/postman` vacío tras regenerar; `grep -c portability src/modules/insurance/README.md` ≥ 1 | TODO |

### H1.S2 — Capa Frontend: modal, descarga dual, a11y y tipos (front)
**CA:** Dado el diálogo de portabilidad, cuando el titular confirma, entonces bajan PDF + JSON; en `loading` no se puede cerrar; los objetivos táctiles miden ≥ 44 px en móvil; el copy no promete lo que no entrega; identificadores en inglés.
**DoD:** specs de `insurance-portability-card/**`, `portability-verify/**`, `insurance-portability.client.spec.ts` y `insurance-portability.handlers.spec.ts` verdes · `yarn typecheck` 0 · `yarn lint` sin problemas nuevos.

| ID | Microtarea | CA (binario) | DoD | Estado |
|---|---|---|---|---|
| H1.S2.M1 | `git cherry-pick c1578610` (copy honesto de #609: 6 archivos); resolver conflictos si aparecen | dev y mockup dicen lo mismo; las 2 pruebas anti-«firma digital» pasan | `yarn test --watch=false --include="src/app/features/account/my-profile/insurance-portability-card/**/*.spec.ts"` → passed | TODO |
| H1.S2.M2 | BUNDLE por defecto: `format = signal('BUNDLE')` (`portability-export-dialog.ts:55`); ajustar `dialog.spec` («Paquete completo preseleccionado») | Confirmar sin tocar el selector descarga PDF y JSON | dialog spec → passed (caso «BUNDLE descarga el PDF y el JSON» sin elegir formato) | TODO |
| H1.S2.M3 | No cerrar en `loading`: `dismissAttempt` se ignora mientras `state()==='loading'`; suscripción del export con `takeUntilDestroyed`; spec: Escape/fondo en loading no emite `closed` | El diálogo sobrevive al request | dialog spec → passed | TODO |
| H1.S2.M4 | Objetivos táctiles: «Copiar hash» y «Descargar PDF/JSON» a `size="md"` (44 px < 780 px, 40 ≥ 780 por el spec del sistema) o `min-height: 44px` bajo 780 en `portability-export-dialog.css` con tokens | En 390×844 ningún botón propio del diálogo mide < 44 px | E2E H1.S3.M4 → passed | TODO |
| H1.S2.M5 | Tipos + maqueta: `PortabilityEncounter` en `insurance-portability.types.ts`; `armarInforme` agrega `encounters` desde los encuentros de la maqueta (`clinical.handlers.ts`) y `schemaVersion: 'alovida.insurance-portability/2'` (hoy `1`, divergente de la API); verify simulado normaliza a minúsculas; el informe se arma con el perfil del **usuario que pide**, no siempre `PACIENTE`; si ninguna persona de `PACIENTES` tiene `coverages: []`, agregar una (sintética, declarada) | La maqueta simula los 3 niveles del contrato (correcto, sin coberturas, hash mayúsculas) | `yarn test --watch=false --include="src/app/core/mock/handlers/insurance-portability.handlers.spec.ts"` → passed (+3 casos) | TODO |
| H1.S2.M6 | lane-29 + testid duplicado: «Volver a descargar» → `data-testid="btn-redownload-portability"`; `portability-hash-copiado→portability-hash-copied`; `id portability-hash-rotulo→portability-hash-label`; BEM `__estado/__descripcion/__rotulo/__copiado/__descargas→__status/__description/__label/__copied/__downloads`, `verify__datos/__rotulo/__volver→__data/__label/__back`; `formatos→formatOptions`, `enLaDireccion→hashFromRoute`, `vigentes→currentCount`; handler mock (`registrarPortabilidadDeSeguros→registerInsurancePortability`, `armarInforme→buildReport`, `reclamosDelInforme→reportClaims`, `periodo→periodStats`, `porAnio→byYear`, `dosDecimales→twoDecimals`, `certificadoDeDemostracion→demoCertificate`, `esTitularOPlataforma→isOwnerOrPlatform`, tipos/const) y sus llamadores; helpers de los specs | `grep -rnE 'hash-copiado|hash-rotulo|__estado|__descripcion|__rotulo|__copiado|__descargas|__datos|__volver|formatos|enLaDireccion|armarInforme|registrarPortabilidad' src/app/{features/account/my-profile/insurance-portability-card,features/insurance/portability-verify,core/mock/handlers/insurance-portability.handlers.ts,core/data-access/insurance}` = 0 | `yarn typecheck` → 0; los 4 grupos de specs → passed | TODO |
| H1.S2.M7 | Card spec: con `isActingForDependent()===true`, el diálogo recibe `auth.patientProfileId()` y `exportPortability` se llama con ese id (nunca `activePatientProfileId`); la alerta tiene `role="status"` y el texto «trámite personal» | El id que viaja es el del titular | card spec → passed | TODO |
| H1.S2.M8 | Docs: `docs/routes/verify-portability.md` y `docs/integrations/backend-api.md:1084` (schemaVersion 2, `encounters`, BUNDLE por defecto) | Docs describen el contrato vigente | `grep -n "insurance-portability/2" docs/routes/verify-portability.md docs/integrations/backend-api.md` ≥ 1 | TODO |

### H1.S3 — Capa Maqueta y E2E Playwright (front, `playwright/carril-insurance-portability.spec.ts`)
**CA:** Dado `yarn start` (maqueta) en `localhost:4200`, cuando corre el spec con `--workers=1`, entonces pasan los casos de doble descarga, dependiente, sin coberturas, táctil/desborde y verificación en 1440×900 y 390×844, con capturas revisadas dos veces.
**DoD:** `E2E_BASE_URL=http://localhost:4200 yarn pw playwright/carril-insurance-portability.spec.ts --workers=1` → todos passed (sin `&`, sin `2>&1`, salida a archivo) · capturas en `evidencia/` · `evidencia/doble-revision.md` con nota por pantalla.

| ID | Microtarea | CA (binario) | DoD | Estado |
|---|---|---|---|---|
| H1.S3.M1 | E2E «paquete completo»: sin tocar el selector, confirmar → dos eventos `download` (uno `.pdf` con `%PDF-`, uno `.json` cuyo `sha256` == sello); ajustar la aserción «PDF preseleccionado» → «Paquete completo» | CA-01 observado en ambos anchos | spec → passed ×2 viewports | TODO |
| H1.S3.M2 | E2E dependiente: `/dependents` → elegir/registrar dependiente (`elegir()`; fecha tecla por tecla: click, `Home`, `pressSequentially`) → `/my-account` › «Seguros y tutores» → `getByRole('status')` con «trámite personal» visible → exportar → sello visible | CA-02 (dependiente) observado | spec → passed ×2 | TODO |
| H1.S3.M3 | E2E sin coberturas: login como la persona sin coberturas → «No tenés coberturas declaradas.» → exportar → sello → `/verify/portability/<hash>` VALID | CA-02 (sin coberturas) observado sin error | spec → passed ×2 | TODO |
| H1.S3.M4 | E2E táctil/desborde: en `ready` medir `btn-copy-portability-hash`, `btn-download-portability-pdf/json`, `btn-redownload-portability` (≥ 44 a 390, ≥ 40 a 1440); `scrollWidth <= innerWidth` con diálogo abierto y en `ready`; capturas `portabilidad-ready-<w>.png`, `verify-<w>.png`, `dependiente-<w>.png` | Sin scroll horizontal; objetivos ≥ 44 en móvil | spec → passed ×2; PNG en `evidencia/` | TODO |
| H1.S3.M5 | Corrida completa del spec + doble revisión (regla 35): pasada 1 verificación, pasada 2 adversarial con las 10 preguntas de `critical-double-review` §3, nota por pantalla | Ninguna pantalla RECHAZADA | salida literal en `evidencia/playwright-portabilidad.txt`; `evidencia/doble-revision.md` | TODO |
| H1.S3.M6 (condicional) | Pierna real: API local (`yarn start:dev` contra Neon) + `yarn start:real-api` → export real → PDF real con QR → `/verify/portability/<HASH MAYÚSCULAS>` VALID. Si la API no levanta en esta máquina, `BLOCKED` con evidencia | CA-01/CA-04 con persistencia real | captura + `curl` del verify en `evidencia/` | TODO |

### H1.S4 — Regresión, PRs y reporte
| ID | Microtarea | CA (binario) | DoD | Estado |
|---|---|---|---|---|
| H1.S4.M1 | Regresión API: `yarn test src/modules/insurance` · `yarn test src/modules/health_data` (por `createExportJob`) · `yarn typecheck` · `yarn lint` | Nada en rojo | salidas literales en `evidencia/` | TODO |
| H1.S4.M2 | Regresión front: `yarn test --watch=false` (suite completa; si esbuild segfaultea, un reintento documentado) · `yarn typecheck` · `yarn lint` | Nada nuevo en rojo (rojos preexistentes se listan con su causa) | salidas literales | TODO |
| H1.S4.M3 | Push de las dos ramas; PR API primero (contrato aditivo), luego PR front referenciándolo; `gh pr create --base dev --reviewer jsaldias39,PabloArauzCaballero` con cuerpo que cite REGISTRO :499 = 6.3·5 = subtarea 3.3 y termine con la atribución; `gh pr view --json mergeable,mergeStateStatus,isDraft,reviewDecision` + `gh pr checks`; verificar que `origin/dev` no avanzó con la rama | Ambos PR `MERGEABLE` y `CLEAN`/`BLOCKED` solo por review humana | salidas literales de `gh` en `evidencia/` | TODO |
| H1.S4.M4 | `REPORTE.md` (avance en la 1.ª línea, Completado/A medias/Pendiente, No cubierto, gates de seguridad/PHI), `docs/tareas/subtarea-3.3-portabilidad-siniestralidad/walkthrough.md` (cierre que faltaba desde el 18/09, apuntando al REPORTE), `claim.py --level` real, memoria del proyecto | Alguien que no vio la sesión puede retomar | archivos en disco; `python .claude/hooks/plan_status.py` coherente | TODO |

## Orden de ejecución
S1.M1→M2→M3→M4→M5→M6 (API, unitario) → S2.M1→M5 (front tipos/mock, para que el contrato coincida) → S2.M2→M3→M4→M6→M7→M8 → S3.M1→M4 (escribir E2E) → S3.M5 (corrida + capturas + doble revisión) → S1.M7 (int-spec contra Neon) → S1.M8→M9 → S3.M6 (pierna real, condicional) → S4. Una microtarea `EN CURSO` a la vez; el PLAN.md en `docs/trabajo/` se actualiza al abrir y cerrar cada una.

## Verificación de punta a punta
- Skill **verify**: cada DoD con comando y salida literal en `evidencia/`; nada se declara por lectura.
- Reglas del arnés: `yarn test [ruta]` (nunca `npx jest`); Playwright con `--workers=1`, sin `&`/`&&`/`2>&1` en el comando, salida redirigida a archivo; `yarn start` debe estar escuchando antes del E2E (no hay `webServer`); revisar `git branch --show-current` antes de cada corrida.
- Seguridad/PHI (regla 90.6): amenaza IDOR → control `assertOwnsPatientProfile` + `INSURANCE_PORTABILITY_DENIED` → test S1.M7 → salida → riesgo residual (bypass de plataforma, `WEB_APP_BASE_URL`).

## Riesgos y bloqueos previstos
| Riesgo | Impacto | Mitigación |
|---|---|---|
| Chromium bloquea la 2.ª descarga automática | S3.M1 en rojo | Dos `waitForEvent('download')` registrados antes del click; si falla, encadenar descargas tras el `download` del PDF (cambio en `downloadChosen`) |
| El int-spec contra Neon deja filas sintéticas y compite con otros usuarios | Datos de prueba en la base compartida | `reset: false`; cuentas con prefijo `portability-e2e-`; `RATE_LIMIT_DISABLED=true` ya lo pone el script |
| Proceso local que mergea a `dev` y cambia de rama el árbol | Evidencia inválida / push a dev sin PR | `git branch --show-current` antes de cada corrida; `git log origin/dev` antes y después de cada push |
| esbuild segfault / `Timeout waiting for worker` en vitest | Falsos rojos | Un reintento documentado; nunca `skip` |
| `cherry-pick c1578610` choca con el `app-select` de C-21 | Conflicto en `dialog.html/spec` | Resolver a mano conservando el select y el copy honesto |
| El cliente que consume el JSON (aseguradora) ve `schemaVersion/2` | Contrato | Cambio aditivo; documentado en README/docs; el front no parsea el certificado |
