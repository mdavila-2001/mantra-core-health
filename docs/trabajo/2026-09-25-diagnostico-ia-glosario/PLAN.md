# Plan — Diagnóstico con IA y glosario masivo (front: D1, D3, D4)

- Fecha: 2026-09-25 · Repos afectados: `mantra-core-health` (este), `AlovidaAIService` (plan propio en ese repo, D2) · Predecesor: C0 (#693) en `origin/mockup` @ `8b50d188`; C3 no entregado
- Plan del paquete (hechos verificados, decisiones P4-1…P4-8, contratos): `AlovidaPromptManager/docs/trabajo/2026-09-25-diagnostico-ia-glosario/PLAN.md`
- Rama: `justin/diagnostico-ia-glosario-2026-09-25` · Worktree: `wt-justin-diagnostico-ia-2026-09-25` · Puerto exclusivo: 4220
- Resultado observable: en `/medical-records/<id>/consultation`, la persona que atiende completa un **Formulario clínico** y, al final, elige una **orden de análisis** y un **diagnóstico tentativo** (sugeridos por la IA a partir de las respuestas); en la casilla **Diagnóstico** ve una **tabla de presuntivos** con estado, enlaza una **orden/informe/nota** como evidencia, escribe la conclusión y **confirma o rechaza**; en `/glossary` la categoría Enfermedades ya no tiene 6 términos sino cientos, con definición cuando existe y código ICD-10-CM siempre.
- Kill-test: `POST /clinical/conditions/:id/verification` sigue respondiendo 404 «Pendiente: carril C3» en el simulador; o `grep -c '"codeSystem":"icd10cm"' data/glossary/*.ndjson` da 0; o el Formulario clínico termina en «Completar formulario» sin ofrecer orden ni diagnóstico.

## Alcance

- IN: `data/glossary/**` (nuevo) · `scripts/{fetch-icd10cm-categories,verify-loinc-codes,check-glossary-corpus}.mjs` (nuevos) · `scripts/gen-glossary-fixture.mjs` · `core/mock/fixtures/{glosario.ts,glosario.generated.ts,glosario.spec.ts}` · `core/mock/fixtures/clinica.ts` (**sólo** la región `CondicionSimulada`/`condiciones`) · `core/mock/handlers/diagnosis-verification.handlers.ts` (+ spec nuevo) · `core/data-access/clinical/clinical.client.ts` (+ spec, **sólo agregar** `verifyCondition`) · `core/data-access/triage-ia/{diagnosis-ia.types,diagnosis-ia.client,diagnosis-ia.client.spec}.ts` (nuevos) · `features/clinical-record/patient-chart/diagnosis-block/**` · `features/clinical-record/patient-chart/diagnosis-verify-dialog/**` (nuevo) · `features/clinical-record/patient-chart/form-conclusion-block/**` (nuevo) · `features/clinical-record/patient-chart/specialty-form-block/specialty-form-block.{ts,html,spec.ts}` (sólo incrustar el bloque y encadenar altas en `completar()`) · `docs/trabajo/2026-09-25-diagnostico-ia-glosario/**` · `package.json` (sólo scripts `mock:glossary:*`).
- OUT: seeds de la API · `app.routes.ts` · `core/navigation/**` · `consultation.ts` (casillas) · `conceptos.ts` · tipos congelados (`clinical.types.ts`, `diagnostics.types.ts`, `chart-notes.types.ts`, `scheduling.types.ts`) · `analysis-order-block/**` · `medical-note-block/**` · los 6 rojos preexistentes de `yarn lint` · `features/glossary/**` (se lee; no se toca salvo que un spec suyo se rompa por el corpus, y en ese caso se corrige el corpus, no el spec).
- Decisiones del usuario (2026-09-25): D1 y D2 en sesiones separadas; el camino con modelo de D2 va en un segundo PR; sin suite completa ni build del front en esta tanda (sólo `--include` y `typecheck`); D3 y D4 para otra tanda.
- Ambigüedades registradas:
  - **Resueltas por el usuario (A1 y A2 del plan del AI service):** el endpoint nuevo es **apoyo al profesional** con disclaimer en toda respuesta; `patient` **sólo viaja al prompt** y no pesa en el puntaje.
  - **A1** «Definiciones de todos los campos» — supuesto: definición clínica + resumen llano + sinónimos + etiquetas + relaciones + síntomas del motor para la capa clínica ES; sólo código + nombre oficial para la capa ancha ICD-10-CM (no se fabrican definiciones sin fuente; regla 97.4). Confirmar con el propietario.
  - **A2** Las relaciones enfermedad→síntoma y enfermedad→prueba de la capa ES no tienen dataset fuente: son orientación curada por desarrollo, marcadas `reviewStatus: "pending-medical-review"` (mismo estatus que la tabla de síntomas del front). **No entran a producción sin revisión médica.** Confirmar con el propietario / equipo médico.
  - **A3** Los nombres ES de la capa clínica son traducciones del título oficial ICD-10-CM/LOINC (que viaja como `enDisplay`), marcadas `translated`. Confirmar con el equipo médico.
  - **A4** «Formulario con preguntas estándar» = las fichas clínicas estándar existentes (43 JSON con procedencia). No se crea otro cuestionario (P4-7). Confirmar con el propietario.
  - **A5** El AI service desplegado en el VPS no tendrá `/v1/diagnosis/suggest` hasta que alguien con SSH corra `deploy/deploy-contabo.sh`; hasta entonces D4 cae al camino manual (el médico elige sin sugerencias) y las pruebas de D4 corren contra un doble (regla 65).

## H1 — Corpus masivo del glosario (D1)

**CA:** Dado el simulador, cuando se abre `/glossary` y la categoría «Enfermedades», entonces lista los 6 curados **más** la capa clínica ES **más** las categorías ICD-10-CM (con código y nombre oficial), y la ficha de un término ES muestra definición, resumen, sinónimos, código externo y relaciones; la de una categoría ICD muestra código, nombre en inglés y la marca de no traducido, nunca una definición inventada.
**DoD:** `corepack yarn mock:glossary` con conteos · `node scripts/check-glossary-corpus.mjs` exit 0 · `npx ng test --include=src/app/core/mock/fixtures/glosario.spec.ts --include=src/app/features/glossary/**/*.spec.ts --watch=false` verde · `corepack yarn typecheck` 0 · capturas de `/glossary` con doble revisión.
**Estado:** A MEDIAS — 10/11: falta sólo H1.S3.M3 (capturas), que exige navegador. Build medido el 2026-09-26: 1,29 MB, dentro del presupuesto. PR #704 mergeado en `mockup` (`2862369d`)

### H1.S1 — Fuentes con procedencia

**CA:** Dado `data/glossary/`, cuando se lee su README, entonces cada archivo declara fuente, URL, fecha, licencia y `reviewStatus`; y ningún código ICD-10-CM o LOINC del corpus es inexistente en su fuente.
**DoD:** `node scripts/fetch-icd10cm-categories.mjs` → 1918 filas · `node scripts/verify-loinc-codes.mjs` → 0 faltantes · `node scripts/check-glossary-corpus.mjs` → 0 errores.
**Estado:** HECHO

| ID       | Microtarea                                                                                   | CA (binario)                                                                                                                                                                                                                                             | DoD (comando de verificación)                                                                                                      | Estado                                                                        |
| -------- | -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| H1.S1.M1 | `data/glossary/00_README.md`                                                                 | Declara las tres capas, fuente/URL/fecha/licencia por archivo, el esquema de fila y los `reviewStatus` posibles                                                                                                                                          | `grep -c "reviewStatus" data/glossary/00_README.md` ≥ 3                                                                            | HECHO                                                                         |
| H1.S1.M2 | `scripts/fetch-icd10cm-categories.mjs` + `data/glossary/cie10cm-categorias.generated.ndjson` | Descarga el ZIP oficial CMS FY2026, lee `icd10cm_order_2026.txt`, escribe las 1918 filas de 3 caracteres (código + título oficial, `lang:"en"`, `source`, sin `definition`)                                                                              | `node scripts/fetch-icd10cm-categories.mjs` → «1918 categorías» · `wc -l data/glossary/cie10cm-categorias.generated.ndjson` = 1918 | HECHO                                                                         |
| H1.S1.M3 | `data/glossary/enfermedades-atencion-primaria.ndjson`                                        | ≥ 120 enfermedades con código ICD-10-CM presente en el order file, `enDisplay` oficial, `esName`, sinónimos, definición ES, resumen ES, `tagKeys`, `symptomIds` del motor y `relations` a pruebas/tratamientos; `reviewStatus: "pending-medical-review"` | `node scripts/check-glossary-corpus.mjs` → «enfermedades: N ≥ 120, 0 errores»                                                      | HECHO                                                                         |
| H1.S1.M4 | `data/glossary/analisis-frecuentes.ndjson` + `scripts/verify-loinc-codes.mjs`                | ≥ 80 análisis (laboratorio e imagen) con LOINC verificado contra el NLM y `category` LAB/IMAGING; nombre oficial guardado como `enDisplay`                                                                                                               | `node scripts/verify-loinc-codes.mjs` → «N/N existen, 0 faltantes»                                                                 | HECHO — lo verifica `verify-external-codes.mjs` (ICD y LOINC juntos): 323/323 |
| H1.S1.M5 | `scripts/check-glossary-corpus.mjs`                                                          | Valida obligatorios, slug único global (curados + NDJSON), `categoryKey`/`tagKeys` de la taxonomía, `symptomIds` existentes en `sintomas.datos.ts`, `relations` resolubles, ICD presente en el order file                                                | `node scripts/check-glossary-corpus.mjs` → exit 0 con conteos por archivo                                                          | HECHO                                                                         |

### H1.S2 — Generador y fixture

**CA:** Dado `yarn mock:glossary`, cuando corre, entonces `glosario.generated.ts` trae los curados + las capas NDJSON con los campos nuevos opcionales, y el simulador los sirve por `GET /terminology/concepts` y `/:id` con `properties` (`external_code`, `code_system`, `lang`, `review_status`, `source`, `symptom_ids`) y `translated` correcto.
**DoD:** `corepack yarn mock:glossary` → conteos · `npx ng test --include=src/app/core/mock/fixtures/glosario.spec.ts --watch=false` verde.
**Estado:** HECHO

| ID       | Microtarea                                              | CA (binario)                                                                                                                                                                                  | DoD (comando de verificación)                                                           | Estado |
| -------- | ------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | ------ |
| H1.S2.M1 | `gen-glossary-fixture.mjs` lee `data/glossary/*.ndjson` | Une seed + NDJSON, reusa la validación de taxonomía/relaciones, escribe `externalCode`, `lang`, `symptomIds`, `reviewStatus`, `source`; falla si un `symptomId` no existe                     | `corepack yarn mock:glossary` → «… términos (69 curados + N clínica + 1918 ICD-10-CM)»  | HECHO  |
| H1.S2.M2 | `glosario.ts`: `terminoEnLinea`/`fichaEnLinea`          | `translated` sale de `lang`; `properties` publica los campos nuevos; `synonyms` incluye `enDisplay` sólo si difiere de `esName`                                                               | `npx ng test --include=src/app/core/mock/fixtures/glosario.spec.ts --watch=false` verde | HECHO  |
| H1.S2.M3 | `fixtures/glosario.spec.ts` (nuevo)                     | Conteos mínimos por capa, slugs únicos, todo `symptomId` existe, toda relación resuelve, todo término tiene categoría válida, ningún término `lang:"en"` trae `clinicalDefinitionEs` no vacía | mismo comando, verde                                                                    | HECHO  |

### H1.S3 — Regresión y pantalla

**CA:** Dado el corpus nuevo, cuando corren typecheck, los specs del glosario y el build, entonces todo queda en verde y dentro del presupuesto; y en el navegador `/glossary` muestra los conteos nuevos y dos fichas (una ES, una ICD-10-CM).
**DoD:** `corepack yarn typecheck` 0 · specs de `features/glossary/**` verdes · `corepack yarn build` dentro del presupuesto · capturas + `evidencia/doble-revision.md`.
**Estado:** A MEDIAS (M3 pendiente)

| ID       | Microtarea                      | CA (binario)                                                                                                                                             | DoD (comando de verificación)                                                                            | Estado                                                                                                                                                |
| -------- | ------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| H1.S3.M1 | typecheck + specs del glosario  | 0 errores; specs verdes sin tocarlos                                                                                                                     | `corepack yarn typecheck` · `npx ng test --include=src/app/features/glossary/**/*.spec.ts --watch=false` | HECHO                                                                                                                                                 |
| H1.S3.M2 | presupuesto del build           | El bundle inicial no supera el presupuesto (1,3 MB); si lo supera, la capa ICD-10-CM pasa a `import()` diferido en el handler                            | `corepack yarn build` → sin «budget exceeded»                                                            | HECHO (2026-09-26) — `corepack yarn build` exit 0; bundle inicial 1,29 MB, igual que `mockup` antes: el fixture va en el chunk diferido del simulador |
| H1.S3.M3 | pantalla `/glossary` en el 4220 | Categoría Enfermedades con conteo nuevo; ficha `asma-bronquial` con definición y código; ficha de una categoría ICD con nombre EN y marca «sin traducir» | capturas en `evidencia/` + doble revisión                                                                | PENDIENTE — corrida aparte con máquina libre (sin navegador en esta tanda)                                                                            |

## H3 — Diagnóstico como tabla de presuntivos → evidencia → conclusión → cierre (D3, entrega C3)

**CA:** Dado un paciente con un diagnóstico presuntivo, cuando quien atiende abre la casilla «Diagnóstico», entonces ve una tabla con estado «En estudio», abre «Confirmar»/«Rechazar», elige una orden, un informe o una nota como evidencia y/o escribe el motivo, completa inicio y fin esperado (o crónico) al confirmar, y la fila pasa a «Enfermedad activa» o «Rechazado»; el simulador responde 404/409/422 como el contrato.
**DoD:** specs del handler, cliente, bloque y diálogo verdes · `corepack yarn typecheck` 0 · recorrido en navegador con capturas y doble revisión.
**Estado:** HECHO — 8/8 (2026-09-26): handler, cliente, tabla y diálogo probados; recorrido en navegador real con 9 capturas en `evidencia/d3/`. Falta sólo la **doble revisión** de las capturas, que no puede ser propia (regla 35.1.6). PR a `mockup` desde `justin/diagnostico-d3-2026-09-26`

### H3.S1 — Simulador y cliente

**CA:** Dado `POST /clinical/conditions/:id/verification`, cuando llega un cuerpo válido, entonces responde 200 con `verification`, `verificationStatusConceptId` y estado clínico según el resultado; e inválido responde 404 / 409 (no provisional) / 422 (sin motivo ni evidencia · evidencia ajena o inexistente · confirmar sin fin ni curso crónico).
**DoD:** `npx ng test --include=src/app/core/mock/handlers/diagnosis-verification.handlers.spec.ts --include=src/app/core/data-access/clinical/clinical.client.spec.ts --watch=false` verde.
**Estado:** HECHO — handler 9 passed · cliente 38 passed

| ID       | Microtarea                                       | CA (binario)                                                                                          | DoD (comando de verificación)                                                                                    | Estado |
| -------- | ------------------------------------------------ | ----------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ------ |
| H3.S1.M1 | `diagnosis-verification.handlers.ts` real + spec | Los 6 caminos del contrato (200 confirmar, 200 rechazar, 404, 409, 422×3)                             | HECHO — 9 passed: 200×3 (confirmar, crónica, rechazar), 404, 409, 422×5                                          | HECHO  |
| H3.S1.M2 | `fixtures/clinica.ts` región condiciones         | `CondicionSimulada.verification` y un presuntivo sembrado por paciente; el resto de la región intacta | HECHO — `verification?` en `CondicionSimulada`; el seed ya trae un presuntivo por paciente (`i > 0`), no se tocó | HECHO  |
| H3.S1.M3 | `ClinicalClient.verifyCondition` + spec          | Mapea `NewDiagnosisVerification` → `POST …/verification` y devuelve `Condition` con fechas            | HECHO — 38 passed (2 nuevas: cuerpo del contrato, fechas como `Date`)                                            | HECHO  |

### H3.S2 — Interfaz

**CA:** Dado el bloque, cuando lista los diagnósticos, entonces muestra tabla con Diagnóstico · Estado (`diagnosisStateOf`) · Evidencia · Acciones; y el diálogo impide enviar sin motivo ni evidencia y sin fin/crónico al confirmar (espejo del 422).
**DoD:** `npx ng test --include=src/app/features/clinical-record/patient-chart/diagnosis-block/**/*.spec.ts --include=src/app/features/clinical-record/patient-chart/diagnosis-verify-dialog/**/*.spec.ts --watch=false` verde.
**Estado:** HECHO — bloque 39 passed (4 nuevas) · diálogo 7 passed

| ID       | Microtarea                              | CA (binario)                                                                                                                                                     | DoD (comando de verificación)                                                                       | Estado |
| -------- | --------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | ------ |
| H3.S2.M1 | `diagnosis-block`: tabla de presuntivos | `app-data-table` con las 4 columnas; estados cargando/vacío/error; el alta sigue naciendo presuntiva                                                             | HECHO — tabla arriba del alta; vacío y error con `ViewState`; relee tras el alta                    | HECHO  |
| H3.S2.M2 | `diagnosis-verify-dialog` (nuevo)       | Confirmar/Rechazar; motivo ≤ 500; evidencia = orden o informe (`getPatientDiagnostics`) o nota; al confirmar: inicio + fin esperado o crónico; errores por campo | HECHO — 7 passed en 3 niveles; evidencia de la persona por `getPatientDiagnostics` + `listNotes`    | HECHO  |
| H3.S2.M3 | acción → diálogo → fila actualizada     | Tras 200, la fila cambia de estado y `cambio` emite; tras 409/422 el diálogo muestra el error accionable sin perder lo escrito                                   | HECHO — clic → diálogo → `alVerificar` → relee y emite `cambio`; 409 mostrado sin perder lo escrito | HECHO  |

### H3.S3 — Verificación

| ID       | Microtarea                         | CA (binario)                                                                                                                                | DoD (comando de verificación)                                                                                                                                                                                                                     | Estado |
| -------- | ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| H3.S3.M1 | typecheck + recorrido en navegador | Registrar presuntivo → «En estudio» → Confirmar con informe → «Enfermedad activa»; Rechazar → «Rechazado»; consola y red sin errores nuevos | typecheck 0 · recorrido real en el 4220 (`evidencia/d3/recorrido-c3.mjs`): En estudio → Confirmar (crónica) → Enfermedad activa; Rechazar → Rechazado; 0 fallos en `/verification`; sin desborde a 375. Doble revisión: pendiente de otra persona | HECHO  |

## H4 — El Formulario clínico termina en orden + diagnóstico tentativo, sugeridos por IA (D4)

**CA:** Dado un formulario clínico de plantilla respondido, cuando quien atiende llega al final, entonces ve «Cierre del formulario» con sugerencias de la IA (o el aviso de que no hay servicio), elige opcionalmente un diagnóstico tentativo y una orden de análisis, y al completar se crean la instancia del formulario, la condición **presuntiva** y la orden, en ese orden, avisando cada fallo sin perder lo escrito.
**DoD:** specs verdes · typecheck 0 · recorrido en navegador con capturas y doble revisión · `REPORTE.md` · PR a `mockup` mergeable.
**Estado:** OTRA TANDA — decisión del usuario (2026-09-25)

### H4.S1 — Cliente de la IA

| ID       | Microtarea                                                | CA (binario)                                                                                                                                                                                                                          | DoD (comando de verificación)                                                                              | Estado     |
| -------- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ---------- |
| H4.S1.M1 | `diagnosis-ia.types.ts` + `diagnosis-ia.client.ts` + spec | `HttpBackend`, sin interceptores, timeout 6 s, `leerSugerencia` valida la forma y devuelve `null` ante error/forma inválida; spec en 3 niveles (correcto, límite: lista vacía y `score` fuera de rango, inválido: HTML del SSR y 502) | `npx ng test --include=src/app/core/data-access/triage-ia/diagnosis-ia.client.spec.ts --watch=false` verde | OTRA TANDA |

### H4.S2 — Bloque de cierre

| ID       | Microtarea                                    | CA (binario)                                                                                                                                                                                                                                                                                              | DoD (comando de verificación)                                                                                                                                                                                            | Estado     |
| -------- | --------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- |
| H4.S2.M1 | `form-conclusion-block` (nuevo)               | Recibe las respuestas; pide sugerencias; estados cargando/sin servicio/vacío/con datos; selector de diagnóstico (`concept-select`, `clinical.conditions.code_concept_id`) y de orden (categoría + `concept-select`), ambos opcionales y precargables desde una sugerencia; emite `{ diagnostico, orden }` | spec verde                                                                                                                                                                                                               | OTRA TANDA |
| H4.S2.M2 | `specialty-form-block`: incrustar + encadenar | El bloque aparece sólo con plantilla elegida; `completar()` encadena `closeInstance` → `createCondition` (presuntivo, `encounterId`, `noteText` con el nombre de la ficha) → `requestStudy` (`category`, `encounterId`); cada paso con su toast; un fallo no deshace el anterior y se dice                | `npx ng test --include=src/app/features/clinical-record/patient-chart/specialty-form-block/**/*.spec.ts --include=src/app/features/clinical-record/patient-chart/form-conclusion-block/**/*.spec.ts --watch=false` verde | OTRA TANDA |

### H4.S3 — Verificación y entrega

| ID       | Microtarea                         | CA (binario)                                                                                                                                        | DoD (comando de verificación)                                     | Estado     |
| -------- | ---------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- | ---------- |
| H4.S3.M1 | typecheck + recorrido en navegador | Ficha con fiebre y tos → sugerencias (o aviso sin servicio) → completar → casilla «Diagnóstico» cuenta uno más y «Orden de análisis» lista la orden | `corepack yarn typecheck` 0 · capturas + doble revisión           | OTRA TANDA |
| H4.S3.M2 | `REPORTE.md` + PR a `mockup`       | Reporte con avance en la primera línea; PR `MERGEABLE`/`CLEAN` o `BLOCKED` sólo por review humana                                                   | `gh pr view <n> --json mergeable,mergeStateStatus,isDraft` pegado | OTRA TANDA |

## Riesgos y bloqueos previstos

| Riesgo                                                            | Impacto                            | Mitigación                                                           |
| ----------------------------------------------------------------- | ---------------------------------- | -------------------------------------------------------------------- |
| Marcelo retoma C3 en paralelo                                     | Conflictos en `diagnosis-block/**` | Anunciado en el daily y en el PLAN del PM; C8 (Justin) integra       |
| `glosario.generated.ts` con ~2 100 términos más pesa en el bundle | Presupuesto 1,3 MB                 | H1.S3.M2: medir; si excede, carga diferida de la capa ICD            |
| La suite completa da verde falso bajo carga                       | Falsa seguridad                    | Sólo specs dirigidos con `--include`, uno por vez; exigir «N passed» |
| El AI service desplegado no tiene `/v1/diagnosis/suggest`         | D4 sin sugerencias en el 8443      | Camino manual siempre funcional; doble local en specs; A5 registrada |
