# Reporte — Perfil del médico: configurar tu perfil (noche 2026-09-22)

> **AVANCE: 83 / 110 — 75,5 %.**

- Fecha: 2026-09-23 · Plan: [PLAN.md](./PLAN.md) · Rama(s): `itzan/perfil-medico-nucleo` (H1–H4) · `itzan/perfil-medico-configurar-tu-perfil` (H5 y H7, fusionada en `mockup` con el #606 el 24/09) · `itzan/perfil-d05-iconos` (H6, la recaptura de H7 y este plan y reporte; [PR #613](https://github.com/mdavila-2001/mantra-core-health/pull/613) contra `mockup`, sobre `b11dfdd3`)
- Corte: `origin/mockup` @ `b7785e36` (movido desde `05d83cb8` el 23/09; ver «Desvíos del plan»)
- Peldaño de evidencia alcanzado: **por hito**, en la tabla de abajo. El del trabajo es el más bajo: `UNKNOWN` (H8 sin empezar)

| Hito | Rama | Peldaño | Nota |
|---|---|---|---|
| H1 | nucleo | `VERIFIED` (sin código propio: corte, baseline y comportamiento previo observados y guardados) | **HECHO.** Corte fijado, typecheck 0, suite completa corrida y sus 7 rojos previos clasificados `ENVIRONMENT` (seis por reproducción aislada), comportamiento previo ejercitado y capturado en navegador |
| H2 | nucleo | `VERIFIED` (specs dirigidos: alta 95/95 · editor 80/80 · ficha 124/124 · detalle 8/8 · Mi perfil 35/35 · insignia, rejilla y pestañas en la corrida de 13 archivos; typecheck 0; compilación de desarrollo sin errores; navegador: 23 capturas miradas y el PATCH visto en la Red) | **HECHO.** En `origin/itzan/perfil-medico-nucleo`: `e72d9290` (código) y `1b86e597` (23 capturas en `docs/frontend/evidence/perfil-medico-datos-y-contacto-2026-09-23/`). Falta la regresión de cierre para `REGRESSION_VERIFIED` |
| H3–H4 | nucleo | `VERIFIED` en lo cerrado (specs del editor 132/132, historial 104/104 y 95/95 tras quitar el adjunto, barra 17/17; typecheck y eslint en 0; tres compilaciones de desarrollo sin errores; navegador: pasada de H3/H4 sin fallos con consola y red limpias, 13/13 consumidores de la barra, 40 capturas miradas) | **EN CURSO**, sin commit todavía. Cerradas 22 microtareas más en el navegador (la última, H3.S4.M5: el historial sin adjunto). Quedan H3.S1.M3 (código de la institución, Q-8) y H4.S3 (filtro «Estado», paginación, alto máximo, acciones de fila, specs y capturas) |
| H5 | configurar-tu-perfil | `TESTED` en conjunto; `VERIFIED` en el editor del paciente y en las altas de paciente, aseguradora, laboratorio e imagenología (navegador 85/85, 20 capturas miradas) | Entregado sin la parte del médico en el [PR #606](https://github.com/mdavila-2001/mantra-core-health/pull/606), fusionado en `mockup` el 24/09. Las dos microtareas del médico (editor y alta) pasan a la rama núcleo, dueña de esos archivos. La segunda pasada de capturas (regla 35), hecha después de la fusión, rechazó las tres altas con dirección obligatoria (H5-01: el campo se pone en rojo apenas el mapa lo vacía; H5-02: en la aseguradora el aviso queda lejos del campo): se abrió H5.S2.M6, que espera una decisión de producto. Dos pruebas de persistencia no se completaron: guardan el alta y la releen, y el inicio de sesión con la cuenta recién creada no respondió en esa corrida |
| H7 | configurar-tu-perfil y perfil-d05-iconos | `VERIFIED` (specs dirigidos 13/13 · 39/39 · 49/49 + navegador 61/61 en los cinco viewports del repo, con las dos pasadas de capturas) | Entregado en el [PR #606](https://github.com/mdavila-2001/mantra-core-health/pull/606) (fusionado el 24/09). La segunda pasada de capturas, hecha después, rechazó la captura del editor (tomada a mitad de una animación) y pidió los viewports que faltaban: se recapturó en la rama de H6 (H7.S1.M7). Quedan dos pantallas `RECHAZADA` por código de otros: la dirección vieja abre «Datos personales» (H7R-01, `app.routes.ts:1072`) y, en el teléfono, la pestaña elegida por la URL queda fuera de la vista (H7R-02, `tabs.ts:88-95`); H7.S1.M6 queda `A MEDIAS` por la segunda |
| H6 | perfil-d05-iconos | `VERIFIED` en los archivos de esa rama: specs dirigidos 234/234, lint, tipos (app y Cypress) y compilación en 0, navegador «antes» 3/3 y «después» 216/216 con 29 + 39 capturas miradas dos veces. Regresión completa corrida (ver «Evidencia»); **no sube a `REGRESSION_VERIFIED`**: la suite queda en rojo por specs ajenos al diff y los umbrales de cobertura no se evalúan | 31 de los 37 `iconOnly` con veredicto (14 excepciones justificadas, 17 a texto: «Editar», los siete botones del calendario y los nueve que quitan la ubicación de un mapa). La segunda pasada de capturas obligó a tres correcciones más: las flechas del calendario en un renglón a 360, el botón del mapa con nombre (y «Cerrar el mapa» cuando todavía no hay punto) y la copia del mapa del alta del paciente. Los 6 restantes son de la rama núcleo |
| H8 | — | `UNKNOWN` | sin empezar |

## Completado

| ID | Qué se logró | Comando | Resultado |
|---|---|---|---|
| H1.S1.M1 | Corte y rama fijados para el bloque H1–H4 | `git fetch origin && git rev-parse origin/mockup && git branch --show-current` | PASS · `evidencia/antes/corte.txt` |
| H1.S1.M2 | Typecheck del corte en 0 | `yarn typecheck` | PASS (exit 0) · `evidencia/antes/typecheck.txt` |
| H1.S1.M3 | Suite completa del corte, guardada | `yarn test --watch=false` | exit 1 · `Test Files 7 failed \| 570 passed (577)` · `Tests 155 failed \| 7066 passed (7221)` · `evidencia/antes/test.txt`; los 7 rojos se clasifican en H1.S1.M4 |
| H1.S2.M1 | Ficha (Datos personales, Contacto) y editor (Trayectoria, Credenciales) capturados en escritorio y móvil, antes de tocar | `yarn node evidencia/antes/capturas-antes.mjs` | PASS · 20 capturas miradas en `evidencia/antes/capturas/INSPECCION.md` |
| H1.S2.M2 | «Editar» abre un modal cuyo «Guardar cambios» está habilitado sin tocar nada, en título, especialidad y matrícula | mismo guion | Observado **SÍ** ×3 · `evidencia/antes/comportamiento.md` |
| H1.S2.M3 | «Retirar» confirma en las tres tablas, con diálogo propio | mismo guion | Observado **SÍ** ×3 · `comportamiento.md` |
| H1.S2.M4 | Insignia «principal»: ficha sí, editor sí, guía (detalle) sí, perfil público **no** | mismo guion + `capturas-antes-paciente.mjs` (la guía es del rol paciente) | Observado · `comportamiento.md` |
| H1.S2.M5 | Consola y red antes de tocar | mismo guion | consola: ninguno · red: sólo mosaicos de OpenStreetMap abortados · `evidencia/antes/consola-red.txt` |
| H1.S1.M4 | Los 7 rojos previos del baseline clasificados: los 7 `ENVIRONMENT` (uno por lectura del error, seis por reproducción aislada: corridos solos pasan enteros y fallan por tope de tiempo o en cascada en la suite completa) | `yarn ng test --include=<spec> --watch=false`, uno por vez | insurance-analytics 10/10 · my-profile 35/35 · agenda-create 64/64 · patient-chart 52/52 · register-organization 57/57 · register-practitioner 95/95 (había dado 1 rojo bajo carga) · `evidencia/antes/aislado-*.txt`; tabla en `PLAN.md` |
| H7.S1.M2 | `/my-account` del paciente tiene cinco pestañas; la quinta monta la billetera dentro de la tarjeta, sin cabecera duplicada; el editor la muestra apagada | `node playwright/mis-puntos-quinta-pestana.mjs <url de la app> despues` | PASS 61/61 (`evidencia/h7/navegador-recaptura.txt`) · la ficha, la pestaña y el editor `ACEPTABLE CON RESERVAS` en la segunda pasada (`evidencia/doble-revision.md`) |
| H7.S1.M5 | Capturas de la ficha, la billetera y el editor, rehechas en los cinco viewports del repo y los dos temas (H7.S1.M7) | inspección, dos pasadas | `docs/frontend/evidence/mis-puntos-quinta-pestana-2026-09-23/README.md` (24 capturas) · `evidencia/doble-revision.md` |
| H7.S1.M1 | `Loyalty` con input `embebido` que omite cabecera y migas; por omisión como antes | `yarn ng test --include=src/app/features/account/loyalty/loyalty.spec.ts --watch=false` | PASS 13/13 (11 previos + 2) · `evidencia/h7/spec-loyalty.txt` |
| H7.S1.M4 | Specs de pestañas de la ficha (cinco, billetera sin cabecera) y del editor (quinta apagada) | `yarn ng test --include=…/my-profile.spec.ts` · `…/patient-profile-edit.spec.ts` | PASS 39/39 · 49/49 · `evidencia/h7/spec-my-profile.txt`, `spec-patient-profile-edit.txt` |
| H7.S1.M6 | `?pestana=<clave>` abre esa pestaña; clave desconocida → la primera | mismo spec de `my-profile` (2 casos) + navegador | PASS · en los archivos de arriba |
| H5.S1.M1 | `app-ubicacion-picker` emite `puntoElegido` en cada toque sobre el mapa, confirmado o no | `yarn ng test --include=…/ubicacion-picker.spec.ts --watch=false` | PASS 17/17 (14 previos + 3) · `evidencia/h5/spec-ubicacion-picker.txt` |
| H5.S1.M2 | Decidido y escrito en el componente: tocar el plano (primer pin o moverlo) emite; «Usar mi ubicación» y quitar no | `grep -n 'D-06' ubicacion-picker.ts` | 2 líneas · en el mismo archivo |
| H5.S1.M3 | Spec de tres niveles: toque, toque sobre pin existente, navegador/confirmar/quitar | mismo spec | PASS · en el archivo de arriba |
| H5.S3.M1 | Quién usa los tres identificadores de confirmación: 3 recorridos de navegador (`toBeVisible`, `waitFor`, `count`), 2 specs (`querySelector`) | `git grep -n -E 'direccion-confirmada\|location-confirmed\|confirmada' origin/mockup -- …` | lista en `evidencia/h5/s3-m1-quien-usa-confirmada.txt` |
| H5.S2.M2 | En el editor del paciente, tocar el mapa del domicilio vacía el campo y avisa justo debajo; la dirección de trabajo no se entera; a 375 sin desborde | `node playwright/mapa-vacia-direccion.mjs` | PASS 44/44 (12 del editor) · `evidencia/h5/navegador-mapa-vacia-direccion.txt` · capturas en `docs/frontend/evidence/mapa-vacia-direccion-2026-09-23/` |
| H5.S2.M3 | En el alta del paciente, cada una de las dos copias del mapa (domicilio y trabajo) vacía su propio campo y avisa | mismo guion | PASS (20 de las 44) · mismo archivo y carpeta |
| H5.S3.M2 | «Listo, guardamos…» ya no se ve en las tres copias: `<p appAnuncio class="solo-lectores">` con el mismo identificador | `git grep -n 'Listo, guardamos' -- 'src/app/**/*.html'` | 3 apariciones, las 3 con `solo-lectores`; typecheck 0 · `evidencia/h5/typecheck.txt` |
| H5.S3.M3 | Las tres pruebas de navegador del equipo que leen la confirmación la siguen encontrando con el texto oculto, sin tocarlas: 11 en verde; en `registro-persistencia` la comprobación (L145) pasa y las dos pruebas caen después, al iniciar sesión contra la API (L86, L166) | `yarn pw playwright/carril-registro-aseguradora-gps.spec.ts playwright/registro-empresa-y-mapa.spec.ts playwright/registro-persistencia.spec.ts --workers=1` | 11 PASS · 2 BLOQUEADO por entorno (ver «No cubierto») · `evidencia/h5/e2e-consumidores-confirmacion.txt` |
| H7.S1.M3 | Pedido a Ender (redirect + retirar renglón) anotado en el daily de Itzan y entregado a Ender como documento aparte, por decisión de Itzan | daily §6/§7 | HECHO |
| H2.S1.M1–M3 | «Estado de la práctica» fuera de la ficha. Antes se midió quién lo usa: quedan usos vivos (pie del sello de la portada, detalle de la guía, tipo), así que no se retira nada más | `git grep -n estadoDePractica` · `git grep -c 'Estado de la práctica' -- src/app` | 0 archivos con el rótulo · usos en `evidencia/h2/estado-de-practica.txt` |
| H2.S1.M4 | La ficha sin esa fila, en verde y vista | `yarn ng test --include=…/practitioner-profile/**/*.spec.ts --watch=false` + `yarn node evidencia/h2/capturas-h2.mjs <url>` | PASS 124/124 (`specs-h2-ficha.txt`) · 6 capturas de Datos personales miradas, sin la fila (`capturas/INSPECCION.md`) |
| H2.S2.M1–M7, M9 | Ninguna especialidad es principal: la insignia tiene un solo tono y no tiene input `principal`; la rejilla no ordena; el editor no tiene «Marcar como principal» ni la columna «Tipo»; el alta no tiene el select de la principal (Q-I6); `isPrimary` queda sólo en tipos y simulador; las otras dos marcas de la ficha se retiraron | `yarn ng test --include=<spec> --watch=false` por archivo · `git grep` del inventario (`principal.txt`, `principal-clasificado.md`, `isprimary-despues.txt`) | PASS · alta 95/95 · editor 80/80 · ficha 124/124 · detalle 8/8 · Mi perfil 35/35 · insignia, rejilla y pestañas en la corrida de 13 archivos (`evidencia/h2/specs-h2*.txt`) |
| H2.S2.M8 | Las insignias se ven iguales en la ficha, el editor, el perfil público, el detalle del directorio (como paciente) y el alta | `yarn node evidencia/h2/capturas-h2.mjs <url>` | PASS · en todas un solo tono (`tone--secondary`), 0 marcas de principal y 0 «principal» en pantalla · `comportamiento-h2.txt`, `capturas/INSPECCION.md` |
| H2.S3.M1–M3, M5, M7 | Contacto sin correo, celular ni fijo del trabajo en la ficha y en el editor; los tres campos del alta, declarados sin pestaña con su motivo (Q-I3) | `git grep -c` de DoD + specs de la vista, el editor, Mi perfil y pestañas | ficha: 0 · editor: sólo comentarios de historia (`grep-dod-h2.txt`) · PASS 124/124 · 80/80 · 35/35 · pestañas en verde |
| H2.S3.M4 | «Correo de acceso» en Datos personales, en sólo lectura, en la ficha y en el editor | mismo guion | Visto en la ficha (3 viewports × 2 temas) y en el editor (escritorio claro, móvil oscuro) · `capturas/INSPECCION.md` |
| H2.S3.M6 | El PATCH del perfil manda sólo lo editado | mismo guion: se deja salir a la red sólo ese envío y se lo retiene ahí | `PATCH /profiles/practitioners/me` · cuerpo `{"mobilePhone":"+591 71234567"}` · 0 claves del trabajo · `evidencia/h2/red-patch.txt` |
| H2.S3.M8 | Contacto y Datos personales en 3 viewports × 2 temas | mismo guion | 12 capturas miradas, 12 OK · consola: sólo el PATCH cortado a propósito · `consola-red-h2.txt` |
| H3.S2.M6 | «Retirar» de títulos confirma antes de borrar | lectura (`retirarFila`, `practitioner-profile-edit.ts:1495-1524`) + lo observado en H1.S2.M3 | Ya lo hacía antes del corte: `dialogs.confirm` · `evidencia/antes/comportamiento.md` |
| H4.S1.M4 | «Retirar» de especialidades confirma antes de borrar | lectura (`.ts:1537-1545` → `retirarFila`) + H1.S2.M3 | Ya lo hacía antes del corte · `evidencia/antes/comportamiento.md` |
| H3.S1.M1, M2, M4 | El alta de un título vive en un modal: sin formulario en línea, «¿Confirmás estos datos?» al agregar, pregunta de descarte con algo escrito y el diploma viaja con el alta | `yarn node evidencia/h4/capturas-h3h4.mjs <url de la app> todo` | PASS · `evidencia/h4/comportamiento-h3h4.txt` · consola y red 0 (`consola-red-h3h4.txt`) · antes de recargar la descarga trae el archivo exacto; después, el simulador devuelve `{}` (ver «Riesgos residuales») |
| H3.S2.M1–M5 | Corregir un título: campos llenos, «Reemplazar el diploma», «Guardar cambios» sólo con cambios, confirmación que devuelve el foco y descarte con cambios; el diploma nuevo reemplaza al original | mismo guion | PASS · `evidencia/h3/capturas/h3-corregir-titulo-*.png` |
| H3.S3.M1, M4 | El historial laboral va en Trayectoria después de los títulos, en tabla, con su propia barra | mismo guion | PASS · 1440 claro y 375 oscuro miradas (`h3-trayectoria-*.png`) |
| H3.S4.M2 | Los 13 consumidores de `app-filter-bar` siguen con la clave `q`, cada uno visto con la cuenta que lo usa | `yarn node evidencia/h3/consumidores-barra.mjs <url de la app>` | PASS 13/13 · consola 0 · `evidencia/h3/consumidores-barra.txt` · 13 capturas `h3-barra-*.png` |
| H3.S4.M4 | El historial en tabla corrige el cargo y retira contra el servidor | mismo guion que H3.S1 | PASS · el cargo sigue tras recargar; tras retirar y recargar, 3 → 2 filas (`h3-historial-*.png`) |
| H4.S1.M1–M3, M6 | Especialidades: alta en modal con casillas sumables y «Quitar» con texto, respaldo sólo con títulos verificados, corrección con guardar por cambios | mismo guion | PASS · las dos especialidades siguen tras recargar · `evidencia/h4/capturas/h4-alta-especialidad-*.png`, `h4-corregir-especialidad.png` |
| H4.S2.M1–M3, M5 | Matrículas: alta en modal con confirmación; la corrección reemplaza el respaldo | mismo guion | PASS · la descarga trae el carnet nuevo y no el original · `h4-alta-matricula-*.png`, `h4-corregir-matricula-con-archivo.png` |
| H4.S3.M2 | Los tres botones de alta van en el hueco de su barra: a la derecha en 1440, debajo en 375 | mismo guion | PASS · cada barra con su clave (`qTitulos`, `q`, `qEspecialidades`, `qMatriculas`) y sin cruzarse |
| H3–H4 (gates tras ajustar los modales) | Specs, tipos, lint y compilación después del último cambio de plantilla y estilos | `yarn ng test --watch=false --include=…/practitioner-profile-edit/*.spec.ts` · `yarn tsc -p tsconfig.app.json --noEmit` · `yarn eslint <10 archivos del diff>` · `yarn ng build --configuration development` | PASS 132/132 (`evidencia/h4/spec-editor-modales.txt`) · tipos 0 (`typecheck-modales.txt`) · eslint 0 (`eslint-modales.txt`) · compilación 0 errores, 8 avisos NG8113 de archivos fuera del diff (`build-dev-modales.txt`) |
| H3.S4.M5 | El historial en tabla deja de ofrecer adjunto (Q-I10): sin columna «Adjunto» ni selector de archivo en «Corregir el vínculo», porque el contrato de afiliaciones no tiene dónde guardarlo | `yarn ng test --watch=false --include=…/work-history/*.spec.ts` (y editor, pestañas) · `yarn tsc -p tsconfig.app.json --noEmit` · `yarn eslint` de los 3 archivos · `yarn ng build --configuration development` · `yarn node evidencia/h4/capturas-h3h4.mjs <url de la app> todo` | PASS · historial 95/95, editor 132/132, pestañas 11/11 · tipos y eslint 0 · compilación sin errores · navegador: columnas «Institución · Período · Acciones», 0 selectores de archivo, el cargo corregido sigue tras recargar, consola y red 0 · `evidencia/h3/specs-h3s4m5-*.txt`, `evidencia/h3/capturas/h3-historial-corregir.png` |
| H6.S1.M4 | El formulario por páginas no cambia para quien lo usa: la botonera ya hidratada es idéntica antes y después en seis pantallas (altas de profesional, organización, laboratorio, centro de imagen y paciente, y la vitrina), y la justificación de las excepciones no llega a la página | `node playwright/d05-iconos.mjs antes` (sobre `mockup` `b11dfdd3`) · `node playwright/d05-iconos.mjs despues` | PASS 12/12 · `evidencia/h6/pasada-{antes,despues}.txt` · `docs/frontend/evidence/d05-iconos-2026-09-23/dom-formulario-por-paginas-{antes,despues}.json` |
| H6.S1.M5 | Specs en verde y capturas miradas dos veces: «Editar» con lápiz y nombre en las ocho celdas (cinco viewports, oscuro en 1440 y 390, más 360), que sigue abriendo el formulario; el calendario con «Cerrar», «Año», «Mes», «Mes», «Año» y «30 años», sin botones de sólo ícono ni desborde, también en la vitrina en modo fecha y hora; el mapa en sus estados | `yarn ng test --include=<spec> --watch=false` · `node playwright/d05-iconos.mjs despues` | PASS · specs 234/234 (`evidencia/h6/specs-h6-m7-m8.txt`) · navegador 216/216, consola y red limpias · 39 capturas con su primera pasada en `docs/frontend/evidence/d05-iconos-2026-09-23/README.md` y la segunda en `evidencia/doble-revision.md` |
| H6.S1.M6 | A cualquier ancho, las flechas de «anterior» del calendario quedan en la mitad izquierda del panel y las de «siguiente» en la derecha | mismo guion (comprobación de mitades) + `date-picker.spec.ts` | PASS en las ocho celdas y en la vitrina · date-picker en verde · commit `f848809a` |
| H6.S1.M7 | Las cuatro flechas del mes del calendario van en un renglón entre 360 y 1920 px, sin escalera | `node playwright/d05-iconos.mjs despues` | PASS «un renglón» en las ocho celdas y en la vitrina · `despues-calendario-dias-360-claro.png` · H6-01 `CERRADO` en la segunda pasada |
| H6.S1.M9 | El calendario en otro consumidor y en su otro modo: la vitrina, en modo fecha y hora, con «Cerrar» y flechas con nombre en un renglón y sin desborde, en 1440, 390 y 360 | mismo guion | PASS · `despues-vitrina-fecha-y-hora-*.png` miradas dos veces |
| H7.S1.M7 | La ficha, la billetera y el editor recapturados en los cinco viewports y los dos temas, con la barra lateral entera | `node playwright/mis-puntos-quinta-pestana.mjs <url de la app> despues` | PASS 61/61 · 24 capturas, dos pasadas · H7-04 resuelto |
| H6.S1.M8 | Los botones del mapa con nombre: «Quitar la ubicación» con punto y «Cerrar el mapa» sin punto, en el mapa compartido y en la copia del alta del paciente, con el nombre accesible completo | specs + `node playwright/d05-iconos.mjs despues` | PASS · specs 234/234 · texto, ícono y nombre en seis estados × 2 viewports (216/216) · H6-02, H6-11 y H6-12 `CERRADO` en la segunda pasada |

## A medias

Por decisión de Itzan (23/09), H5 se entrega en un PR propio contra `mockup` sin la parte del médico, para no encadenarlo con el de H2; el médico va en un PR aparte cuando los dos entren a `mockup`.

### H5.S2.M4 — Las altas que montan el mapa vacían su dirección
- Qué anda: aseguradora, laboratorio e imagenología (central y cada sucursal), vistas en el navegador (`evidencia/h5/navegador-mapa-vacia-direccion.txt`, capturas en `docs/frontend/evidence/mapa-vacia-direccion-2026-09-23/`).
- Qué no anda: el alta del médico todavía no vacía sus dos direcciones (consultorio y domicilio) al tocar el mapa.
- Qué falta exactamente: en `register-practitioner.html` conectar `(puntoElegido)` de los dos `app-ubicacion-picker` a un vaciado de cada campo con su aviso (el mismo patrón que `register-organization`), un caso de spec y el recorrido en el guion.
- Dónde quedó: esta rama no toca `register-practitioner`; se hace sobre `mockup` cuando entren H2 y este PR. Compila.

### H5.S2.M5 — Spec de al menos dos padres
- Qué anda: el spec del alta de paciente (117/117) y el del editor del paciente (50/50) cubren el vaciado.
- Qué no anda: falta el caso en el spec del editor del médico, que el plan nombra.
- Qué falta exactamente: el caso en `practitioner-profile-edit.spec.ts` junto con H5.S2.M1.
- Dónde quedó: igual que la anterior.

### H6.S1.M1–M3 — Veredicto de los 37 `iconOnly`, texto donde no hay excepción y justificación donde sí
- Qué anda: los 31 de `register-patient`, `paginated-form`, `date-picker`, `back-link`, `ubicacion-picker`, `patient-profile-edit` y `my-profile` tienen veredicto (`evidencia/h6/iconos.md`). Los 17 que no son excepción ya llevan ícono + nombre y están vistos en el navegador. Las 14 excepciones tienen su línea «Excepción ADR-0012 §3 (D-05, 23/09)» al lado (`evidencia/h6/grep-iconOnly-con-motivo.txt`).
- Qué no anda: nada de lo hecho. Faltan los 6 de la rama núcleo: `register-practitioner` 3, `practitioner-profile-edit` 2 y `practitioner-profile-view` 1.
- Qué falta exactamente: el veredicto, el cambio y la justificación de esos 6, en la rama núcleo, que es la dueña de esos archivos.
- Dónde quedó: rama `itzan/perfil-d05-iconos`, sobre `mockup` `b11dfdd3`, en el PR de H6; compila (tipos 0, compilación de producción 0).

### H6.S1.M10 — El PR de H6, mergeable (regla 35.2)
- Qué anda: el [PR #613](https://github.com/mdavila-2001/mantra-core-health/pull/613) contra `mockup` da `mergeable: MERGEABLE`, sin draft y sin conflictos.
- Qué no anda: `mergeStateStatus: UNSTABLE`. Sus tres checks (`verificar`, `dependencias`, `e2e`) están pendientes, sin fallar: todas las corridas del CI del repo, de cualquier rama y también de `dev`, siguen en «queued» sin arrancar (clase `EXTERNAL`, `evidencia/pr-613-mergeable.txt`).
- Qué falta exactamente: que el CI vuelva a tener quién lo ejecute, y volver a consultar `gh pr view 613` y `gh pr checks 613`. Mientras tanto, los pasos del CI están corridos a mano sobre el árbol final (`evidencia/h6/cierre-*`).
- Dónde quedó: publicado; la revisión humana es la que manda el repo.

### H7.S1.M6 — La ficha abre la pestaña que nombra la URL
- Qué anda: `/my-account?pestana=puntos` abre la billetera con «Mis puntos» seleccionada, en el spec y en el navegador (1440 y 390); una clave desconocida cae en la primera pestaña.
- Qué no anda: en el teléfono, la pestaña seleccionada queda fuera de la parte visible de la tira; la segunda pasada rechazó esa pantalla (H7R-02, `MAYOR`). La tira de pestañas sólo se corre hasta una pestaña cuando la persona la elige (`src/app/shared/components/molecules/tabs/tabs.ts:88-95`); una selección que llega por `[(selectedIndex)]` no la trae a la vista.
- Qué falta exactamente: que la molécula de pestañas traiga a la vista la pestaña elegida también cuando la selección llega desde afuera, y volver a capturar la entrada por la URL a 390. La molécula es compartida y no es de este carril: se propone a quien la lleve.
- Dónde quedó: el código de la ficha, en `mockup` desde el #606; compila.

## Pendiente

| ID | Estado | Qué lo destraba |
|---|---|---|
| H3.S1.M3 | TODO | nada: el catálogo de instituciones no tiene código (la sigla vive en el texto de la opción, `core/profesion/instituciones-educativas.ts:35`); se agrega el dato y se muestra en la columna (Q-8) |
| H4.S3.M1, M3–M7 | EN CURSO / TODO | nada: filtro «Estado», paginación, alto máximo (Q-I9), specs, capturas 3 × 2 y acciones de fila |
| Commit y PR de la rama núcleo | TODO | cerrar H3/H4 y normalizar las rutas de los logs de `evidencia/antes`, `h2`, `h3` y `h4` |
| H5.S2.M1 | TODO | que H2 y el PR de H5 entren a `mockup` (decisión de Itzan, 23/09); después, un PR aparte con el editor y el alta del médico |
| H5.S2.M6 | TODO | una decisión de producto sobre H5-01 y H5-02 (no marcar en rojo el campo que el mapa vacía hasta que la persona lo toque; acercar el aviso al campo en la aseguradora). Después, specs de cada alta y capturas mirada dos veces |
| H8 | TODO | que los PR del carril estén abiertos |

## Evidencia

- `evidencia/antes/` — corte y baseline del bloque H1–H4 (`corte.txt`, `lint.txt`, `typecheck.txt`, `test.txt`), comportamiento previo ejercitado (`comportamiento.md`, `consola-red.txt`, guiones `capturas-antes*.mjs` con su salida `.txt`) y 20 capturas miradas en `capturas/INSPECCION.md`.
- `evidencia/h2/` — specs dirigidos (`specs-h2*.txt`), typecheck (`typecheck-h2*.txt`), compilación de desarrollo (`build-dev-h2.txt`), eslint sobre los 26 `.ts`/`.html` del diff (`lint-h2.txt`, exit 0), inventarios de `principal`, `isPrimary` y `estadoDePractica`, los grep de DoD (`grep-dod-h2.txt`), el recorrido de navegador `capturas-h2.mjs` con su salida (`capturas-h2.txt`, `comportamiento-h2.txt`), `consola-red-h2.txt`, `red-patch.txt` y 23 capturas + `capturas/INSPECCION.md`. Las mismas 23 capturas viajan en el repo, en `docs/frontend/evidence/perfil-medico-datos-y-contacto-2026-09-23/`.
- `evidencia/h3/` — specs de cada paso (`specs-h3s1.txt`, `specs-h3s2.txt`, `specs-h3s4-*.txt`, `specs-h3s4m5-*.txt`), lint y tipos (`lint-h3s*.txt`, `eslint-h3s4m5.txt`, `typecheck-h3s*.txt`), formato del historial (`prettier-h3s4m5.txt`), la compilación de desarrollo tras quitar el adjunto (`build-dev-h3s4m5.txt`), `arquitectura-h3s1.txt`, el recorrido de los consumidores de la barra `consumidores-barra.mjs` con su salida (`consumidores-barra.txt`, `consumidores-barra-consola.txt`) y 27 capturas en `capturas/` (14 del editor y 13 de consumidores).
- `evidencia/h4/` — el recorrido de H3/H4 `capturas-h3h4.mjs` con su salida (`comportamiento-h3h4.txt`, `consola-red-h3h4.txt`), el desborde medido a 375 (`desborde-375.txt`), specs, lint y tipos (`specs-h4-editor.txt`, `spec-editor-modales.txt`, `lint-h4.txt`, `eslint-modales.txt`, `typecheck-h4.txt`, `typecheck-modales.txt`, `typecheck-spec-h4.txt`), dos compilaciones de desarrollo (`build-dev-h4.txt`, `build-dev-modales.txt`) y 13 capturas en `capturas/`.
- `evidencia/h7/navegador.txt` — salida literal del guion de navegador del #606 (18/18) y la primera corrida fallida con su clase. `evidencia/h7/navegador-recaptura.txt` — la recaptura del 24/09 en los cinco viewports del repo (61/61, más dos mediciones informativas con resultado negativo: los hallazgos H7R-01 y H7R-02).
- `docs/frontend/evidence/mis-puntos-quinta-pestana-2026-09-23/` — 24 capturas + `README.md` con la primera pasada (recapturadas el 24/09 en la rama de H6).
- `evidencia/doble-revision.md` — la segunda pasada de capturas (regla 35), en sus rondas: H5 y H7 sobre las capturas del #606; H6 y la recaptura de H7, repetida hasta que ninguna pantalla de este cambio quedó `RECHAZADA`.
- `evidencia/pr-613-mergeable.txt` — salida literal de `gh pr view 613 --json …` y `gh pr checks 613` después del último push, con el listado de corridas del CI que muestra que ninguna arranca.
- `evidencia/h5/` — specs dirigidos (`spec-ubicacion-picker*.txt`, `spec-patient-profile-edit.txt`, `spec-register-*.txt`), `typecheck.txt` y `navegador-mapa-vacia-direccion.txt` (85/85), `navegador-imagenologia-repeticion.txt` (22/22) y `e2e-consumidores-confirmacion.txt` (11 PASS, 2 bloqueadas por entorno). Capturas en el repo: `docs/frontend/evidence/mapa-vacia-direccion-2026-09-23/` (20, con la inspección de cada una en su `README.md`).
- `evidencia/h5/cierre-*` — gates de cierre del PR #606, corridos sobre la rama rebasada en `origin/mockup` @ `a43ad2b3`:
  - `cierre-lint.txt` (0), `cierre-tipos.txt` (0), `cierre-typecheck.txt` (`yarn typecheck`: app + Cypress + Playwright, 0), `cierre-tipos-cypress.txt` (0).
  - `cierre-suite-cobertura.txt`: `Test Files 4 failed | 579 passed (583)` · `Tests 4 failed | 7333 passed (7337)`.  - `cierre-4-rojos-en-rama.txt` y `cierre-4-rojos-en-mockup.txt`: los 4 rojos corridos aparte dan la misma salida en la rama y en `mockup`, `1 failed | 3 passed (4)`. Sólo `mock-backend-latencia.spec.ts` falla, con `ENOENT` (`join(__dirname, 'mock-backend.interceptor.ts')`, L82, commit 59c4cfb0 de Pablo): `TEST_BUG` heredado. Los otros tres (`my-agenda`, `insurance-analytics.handlers`, `insurance-portability.handlers`) pasan aislados: `ENVIRONMENT` por carga.
  - `cierre-build.txt` (0), `cierre-bundle-budget.txt` (0).
  - `cierre-checks.txt` y `cierre-check-*-en-mockup.txt`: architecture, api-prefixes, form-pages y css-tokens en rojo con la misma salida en `mockup`; route-prefixes, tokens y contrast en 0.
  - `cierre-doc-report.txt` y `cierre-doc-report-en-mockup.txt`: 7 de 11 en rojo en los dos. La salida entera difiere en una sola línea: el conteo de páginas, 347 contra 345, por los dos README de evidencia.
- `evidencia/h6/` — veredicto por fila (`iconos.md`), justificación al lado de cada excepción (`grep-iconOnly-con-motivo.txt`), specs (`specs-h6.txt` 331/331 del 23/09; `specs-h6-m7-m8.txt` 234/234 del 24/09, sobre los cuatro archivos tocados por las correcciones), `lint-*` y `typecheck-h6.txt`, `eslint-h6-m7-m8.txt` y `tipos-h6-m7-m8.txt` (0) y las dos pasadas de navegador: `pasada-antes.txt` (3/3, sobre `mockup` `b11dfdd3`) y `pasada-despues.txt` (216/216). Capturas en el repo: `docs/frontend/evidence/d05-iconos-2026-09-23/` (29 de referencia y 39 del cambio, con la primera pasada en su `README.md`).
- `evidencia/h6/cierre-*` — gates de cierre del PR de H6 sobre el árbol final:
  - `cierre-lint.txt` (0), `cierre-tipos.txt` (0), `cierre-tipos-cypress.txt` (0).
  - `cierre-suite-cobertura.txt`: `Test Files 2 failed | 581 passed (583)` · `Tests 4 failed | 7373 passed (7377)`. Los dos archivos en rojo son ajenos al diff (la rama no toca `src/app/core/`). `mock-backend-latencia.spec.ts` da `ENOENT` al abrir `mock-backend.interceptor.ts` desde la raíz del repo (L82): `TEST_BUG` heredado, igual que en `mockup` (`evidencia/h5/cierre-4-rojos-en-mockup.txt`; el spec no cambió entre `a43ad2b3` y `b11dfdd3`). `fallos-simulados.spec.ts` cae con `QuotaExceededError` del almacenamiento de las pruebas en la corrida completa y pasa aislado (`cierre-rojos-aislados-en-rama.txt`); en la corrida anterior sobre el mismo `core/` había pasado: `ENVIRONMENT`, por el reparto de specs entre workers. Con la suite en rojo, los umbrales de cobertura no se evalúan.
  - `cierre-build.txt` (0): `date-picker.css` ya superaba su presupuesto en `mockup` (7,17 kB en `evidencia/h5/cierre-build.txt`; el archivo no cambió en `mockup` desde entonces) y queda en 7,31 kB.
  - `cierre-checks.txt` contra `cierre-checks-en-mockup.txt`: architecture, api-prefixes, form-pages y css-tokens en rojo con la misma salida que en `mockup`; bundle-budget, route-prefixes, tokens y contrast en 0; doc-report difiere sólo en la sección del bundle.

## No cubierto

- H2: el listado de una especialidad (`/directory?especialidad=…`) no se capturó. No dibuja insignias ni menciona «principal» (0 coincidencias en el componente y en `directory-page`), así que D-01 no tiene nada que mostrar ahí.
- H2: el alta se miró con el formulario puesto directamente en su página de especialidades (12 de 13), sin recorrer las once anteriores; el recorrido completo lo cubre `register-practitioner.spec.ts` (95/95).
- H2: el PATCH se vio salir con un solo campo editado (el celular); las combinaciones de varios campos las cubre el spec del editor (cuerpo exacto con los tres campos del trabajo cargados).
- H5: en el navegador se vieron siete mapas (editor del paciente; altas de paciente ×2, aseguradora, laboratorio central y sucursal, imagenología central y sucursal) en 1440 claro; además el editor en 1440 oscuro, 375 y 768, y el alta de paciente en 1440 oscuro. **No se miró** el tema oscuro de aseguradora, laboratorio e imagenología, ni sus altas en 375/768.
- H5: las dos pruebas de `registro-persistencia` quedan **BLOQUEADAS**: son de integración, guardan el alta y la releen contra la API real, y en esa corrida el inicio de sesión no obtuvo respuesta (`ECONNREFUSED`, L86 y L166). La parte que toca H5 (L145, la confirmación) pasó antes del fallo.
- H5: las cuatro capturas de imagenología salen de una repetición de ese recorrido (22/22, `evidencia/h5/navegador-imagenologia-repeticion.txt`): en la pasada general la de la central salió en blanco, tomada antes de que la tarjeta se pintara.

- H7: la billetera **con saldo, canje y comprobante** dentro de la pestaña no se observó: la cuenta sintética no tiene programa activo y la maqueta responde vacío. Esos estados los cubre `loyalty.spec.ts` para la pantalla propia; dentro de la pestaña sólo se vio el vacío.
- H7: la dirección vieja `/my-account/loyalty` ya redirige, pero a `/my-account` (`src/app/app.routes.ts:1072`, de otro carril, 23/09): abre «Datos personales» y no la billetera. Se probó la pestaña por URL directa y se midió adónde lleva la dirección vieja; el destino `?pestana=puntos` es de quien lleva esa ruta.
- H6: el calendario no se recorrió con el teclado en el navegador; el orden (el mes antes que las flechas) y la trampa de foco los cubre `date-picker.spec.ts`. Tampoco se probó con un lector de pantalla real.
- H6: el calendario se vio en dos de sus 22 consumidores (el editor del paciente y la vitrina, en sus dos modos) y no dentro de otro diálogo; el mapa compartido, en uno de sus seis consumidores. Ninguna captura muestra una excepción con su globo abierto. El tema oscuro se vio en 1440 y 390; el mapa y la vitrina, sólo en claro.
- H6: la regresión de Cypress (`test:e2e:smoke`, `critical` y la completa) no se corrió.
- H7: aislado, `my-profile.spec.ts` da 39/39; en la suite completa del corte (rama núcleo, `evidencia/antes/test.txt`) ese archivo tenía 69 rojos con «Cannot configure the test module when the test module has already been instantiated», previos a este cambio. Se clasifican en H1.S1.M4.

## Desvíos del plan

- **Corte movido** de `05d83cb8` a `b7785e36` antes de escribir código (decisión de Itzan, 23/09): el PR #582 trajo `confirmarCambios`/`confirmarDescarte`, la pieza D-08 que H3/H4 esperaban de Marcelo. Esas microtareas se cierran contra la pieza real, no contra `dialogs.confirm` como doble.
- **H7.S1.M6 agregada** (regla 00 §3.3): `my-profile` no leía la ruta, y sin eso el redirect de H7.S1.M3 caería en «Datos personales».
- **Decisión H7.S1.M2**: el editor muestra «Mis puntos» deshabilitada, no la oculta (la constante compartida existe para que las posiciones no cambien entre lectura y edición).
- **CA de H2.S3.M5 reescrito (Q-I3)**: decía «el spec de pestañas pasa sin tocarlo», imposible porque `pestanas-del-perfil-medico.spec.ts:64` fija el conjunto exacto de ausencias. Ahora: L64 pasa a las cinco claves y se afirma el motivo de las tres nuevas, como en L65-66. Confirma Pablo.
- **CA de H2.S2.M5 precisado (Q-I6)**: `specialtyPrimary` sale del alta y del mapa de pestañas; el spec no se toca. La primera especialidad agregada queda como principal para el backend sin que nadie la elija (contrato intacto, Q-1).
- **Cinco filas de H3/H4 se cierran contra la pieza real** (H3.S1.M4, H3.S2.M4, H3.S2.M5, H4.S1.M3, H4.S2.M3): `confirmarCambios`/`confirmarDescarte` de `molecules/dialog` (`dialog-service.ts:72` y `:92`), llegados con el corte. Ya no hay doble `dialogs.confirm` que declarar.

- **Guion de navegador de H5 corregido antes de valer como evidencia** (nada de esto cambia la aplicación): los campos del alta llevan el identificador en el `<input>` mismo; con un punto ya guardado el mapa abre desplegado y no hay botón «Marcar»; el toque por coordenadas no llegaba a un mapa fuera de la ventana; `innerText` incluye el texto recortado, así que «no se ve» se mide por la caja de cada nodo; las capturas se toman arriba de la página, con movimiento reducido y el cursor lejos del menú (antes salían con la cabecera fija a mitad de imagen y el menú a medio abrir); y los textos escritos eran idénticos a los ejemplos de los campos, lo que volvía ambigua la captura. Después, para laboratorio e imagenología: cada intento de «Siguiente» vuelve a completar el paso (lo escrito antes de que la página termine de hidratarse se pierde), la casilla de estudios se marca por su etiqueta, y antes de cada captura se espera a que no quede ninguna transición en curso (una captura oscura había salido a mitad del cambio de tema). Quedan tres esperas fijas heredadas del recorrido de alta existente (fecha con máscara y arranque del formulario).

- **La rama núcleo integró `mockup` (`a43ad2b3`) con un merge (`5c621ac3`) antes de empezar H3, y H3–H4 se re-midieron contra ese árbol.** Entre el corte y `mockup` entraron las piezas de tablas de Pablo (`9858520f`: hueco de acción en la barra, paginador con texto y salto, alto máximo en la tabla, historial laboral como tabla), `closeGuard` en el modal y la corrección de vínculos laborales. Cambios en el PLAN: H3.S2.M6 y H4.S1.M4 pasan a HECHO (ya confirmaban antes del corte); H3.S3.M2 queda DESCARTADA (la tabla llegó); H3.S3.M1 queda BLOQUEADA; H3.S2.M2 y H4.S2.M2 se achican al editor (el tipo y el simulador ya aceptan `fileId`); las rutas y líneas del editor se actualizaron; se agregó H4.S3.M7 (acciones de fila, ADR-0012 §2) y el total pasa a 96. Q-I8 y Q-I9 registradas.
- **H3.S4.M4, retiro desde la tabla**: en vez de duplicar confirmar + `DELETE` + releer, la tabla reusa `retirarVinculo` de la línea de tiempo. Su error sólo se leía dentro del modal del alta, que al retirar está cerrado; se le sumó un aviso (`toasts.error`). Eso cambia también la línea de tiempo: un retiro fallido ahora se avisa, antes no decía nada.
- **Lint heredado en `filter-bar.spec.ts`**: HEAD ya tenía 4 errores de `prefer-on-push-component-change-detection` en sus componentes de prueba (llegaron con #588). Como ese archivo entra en el alcance que encargó Pablo y el gate pide lint en 0, se pusieron en `OnPush` los 5 (los 4 heredados y el nuevo); el spec sigue en 17/17.
- **«Quitar la especialidad N» pasa a ícono + texto**: al mover el alta de especialidades a un modal, ese botón de sólo ícono quedó dentro de un `<dialog>` nativo, donde el globo de `appTooltip` queda tapado (confirmado en el navegador: el globo se cuelga del `<body>`, `tooltip.ts:131`). Se le puso el texto «Quitar» a la vista y se conservó el `aria-label` con el número. Mismo criterio que se aplicó al calendario (Q-I12).

- **H6.S1.M4 compara el DOM, no capturas**: el CA decía «5 capturas comparadas». Se comparó el HTML de la botonera ya hidratada en 6 pantallas, que es más estricto que mirar dos imágenes. La lista de pantallas del guion se corrigió en el camino: las altas de acceso delegado no montan el formulario por páginas con esas cuentas, así que se usaron las altas públicas y la vitrina.
- **H6.S1.M6 agregada** (regla 00 §3.3): el defecto de las flechas a 375 apareció al mirar las capturas.
- **Guion de H6 corregido antes de valer como evidencia**: leía las flechas de la grilla de años antes de que cambiara el panel, y la botonera antes de que terminara la hidratación (los `jsaction` del servidor aparecían en unas lecturas sí y en otras no). Con esas dos esperas se volvió a tomar la referencia del «antes». El desborde de «Mi perfil» se mide ahora en las dos pasadas, porque a 375 ya había 1 px antes del cambio.
- **H6.S1.M7, M8 y M9 agregadas** (regla 00 §3.3) por la segunda pasada de capturas: las flechas del calendario en escalera a 360 (H6-01), el aro con un menos del mapa tomado como excepción (H6-02) y el calendario visto en un solo consumidor (H6-E1). La revisión se repitió después de cada corrección, en cuatro rondas, y amplió M8 dos veces: la copia del mapa del alta del paciente y el estado sin punto. Los CA de M7 y M8 se corrigieron a lo medido (decían 320 px y «Quitar la ubicación» en los tres estados).
- **H6 pasa a los viewports del repo**: 1440, 1920, 1024, 768 y 390, con oscuro en 1440 y 390, más 360 (donde el calendario es más estrecho). El 23/09 se había capturado en 375, 768 y 1440.
- **La recaptura de H7 (H7.S1.M7) va en la rama de H6**: el #606 ya estaba fusionado; el guion de H7 se corrigió (sin `fullPage`, con el puntero fuera de la tira y esperando a que la billetera resuelva su estado) y la dirección vieja pasó de «sigue con su cabecera» a «ya no pinta una pantalla aparte», porque en `mockup` ahora redirige.
- **La grilla de los modales del editor se topa en dos columnas** (hallado al mirar las capturas, regla 00 §3.3): la grilla del editor sube de columnas según el ancho de la ventana, no el del contenedor, y con la ventana ancha partía un modal `md` en tres columnas de unos 195 px, donde no entraban el nombre de una universidad ni una especialidad con su «Quitar». `.edicion__formulario--modal` la deja en dos, y las especialidades del alta ocupan el renglón entero.
- **La barra de acciones de los modales deja los estilos de barra de página** (hallado igual): `app-form-actions` dentro del pie del diálogo dibujaba una segunda línea en 1440; en teléfono quedaba pegada al fondo con fondo propio y 12 px por fuera del pie, y a 320 px, con «Agregar especialidades», «Cancelar» quedaba 37 px fuera de la pantalla. `.edicion__acciones-modal` la pone estática, sin borde ni margen, y la deja bajar de renglón como el propio pie. Medido otra vez: a 1440, 375 y 320 las acciones caen dentro del pie.

## Riesgos residuales

- **Sin consumidor desde D-01 (H2.S2.M7)**: en el simulador quedan `PrincipalElegida` y el `PATCH …/specialties/:id/primary` (`core/mock/handlers/profiles.handlers.ts:85-97, 160-172, 1061-1064, 1135-1136`), y en el cliente `setOwnPrimarySpecialty` (`profiles.client.ts`). Nadie los llama: el editor retiró «Marcar como principal». Se conservan porque la API expone el mismo camino; si el contrato retira `isPrimary`, se van con él.
- **Los correos del médico se contradicen entre el alta y la lectura (hallazgo, no se toca en este carril)**: el alta manda el correo **personal** como `email` —el campo de acceso— y el de trabajo como `workEmail` (`register-practitioner.ts:2359-2370`, con su propio aviso de que la API todavía no declara `workEmail`); la lectura documenta `email` como «el de trabajo y a la vez el de acceso» y trae `personalEmail` aparte (`profiles.types.ts:383-390`), y el simulador devuelve `email` = `workEmail` = la cuenta. «Correo de acceso» (H2.S3.M4) muestra `perfil.email`, que es el campo de acceso en las dos lecturas, así que no depende de cuál gane; lo que sí queda ambiguo es qué muestra «Correo personal» en «Contacto» cuando la cuenta nació con el alta nueva. Para quien lleve el contrato de perfiles.
- **`/loyalty` sin rutear en el despliegue (heredado, `check-api-prefixes`)**: el prefijo está en `proxy.conf.json` pero no en `proxy.conf.docker.json` ni en `deploy/api-locations.conf` + `nginx.conf`. «Mis puntos» usa el mismo cliente que la pantalla de puntos, así que en un despliegue que siga esos archivos la pestaña llamaría a una ruta que el proxy no conoce. Se avisó en el PR #606; queda para quien lleva el despliegue.
- `my-profile.spec.ts` recorre todas las pestañas al probar la ficha; la quinta monta la billetera. Se le dio el doble del cliente de puntos (sin programa) para que no pida red. Si alguien cambia el vacío de la billetera, ese spec lo va a ver.
- Las dos microtareas de H5 que esperan el push de H2 tocan archivos que H2 también toca: se rebasa después, no antes.
- **La pista de «Dirección» en Contacto del editor habla del trabajo (hallazgo previo, no se toca en este carril)**: `practitioner-profile-edit.html:248` dice «Dónde atendés o dónde te ubican tus pacientes.» dentro de Contacto, que desde D-03 es sólo personal. Ya estaba en el corte (L231) y queda fuera del CA de D-03, que nombra correo, celular y fijo del trabajo. Queda para quien decida el texto.
- **Dos textos para el mismo gesto en la pestaña Trayectoria: resuelto.** El historial descartaba con un `dialogs.confirm` genérico; con H3.S4.M6 (encargo de Pablo) los tres descartes usan `confirmarDescarte`, el mismo texto que las tablas del editor.
- **Hallazgo (historial en modo tabla, fuera de las tres correcciones encargadas):** el alta «Añadir elemento a tu historial» queda como botón suelto debajo de la tabla (`work-history.html:570-581`), no proyectado a la derecha de la barra como pide la regla 5 del ADR-0015; y con `layout="tabla"` el bloque va dentro de una tarjeta (`work-history.ts:213-215` sólo la quita para la línea de tiempo). Visto en el navegador ya montado en Trayectoria: el alta sigue fuera de la barra y la tarjeta queda dentro de otra. Se le propone a Pablo. En el mismo historial, la cruz del modal de corrección dice «Cancelar», y la de los modales del editor, «Cerrar».

- **El globo de ayuda no se ve dentro de un diálogo modal** (hallazgo, confirmado en el navegador): `atoms/tooltip/tooltip.ts:131` cuelga el globo del `<body>` y un `<dialog>` con `showModal()` se pinta por encima de todo. Ningún botón de sólo ícono dentro de un modal nativo cumple el «globo visible» del ADR-0012 §3. Para quien lleve el átomo.
- **Laboratorio y centro de imagen encienden las flechas de sólo ícono sin su justificación** (`register-laboratory.html:67`, `register-imaging-center.html:68`): fuera de los 37 del encargo. Les toca el mismo veredicto que al alta del paciente (Q-I11).
- **«Mi perfil» desborda 4 px a 360** (heredado, medido antes y después de H6; a 390 y más, 0): el avatar del encabezado sale cortado contra el borde (`div.app-header__derecha`).
- **La dirección vieja de «Mis puntos» abre la ficha en la primera pestaña** (hallazgo, otro carril): `/my-account/loyalty` redirige a `/my-account` (`src/app/app.routes.ts:1072`) y el comentario de `:1065-1069` dice que es «hasta que la pestaña exista». Ahora existe, y la entrada `?pestana=puntos` funciona. Para quien lleve esa ruta.
- **La tira de pestañas no trae a la vista una selección que llega desde afuera** (hallazgo, molécula compartida): `src/app/shared/components/molecules/tabs/tabs.ts:88-95`. Cuando la dirección vieja redirija a `?pestana=puntos`, quien entre desde el teléfono va a ver la billetera sin su pestaña marcada a la vista. Para quien lleve la molécula.
- **A 360, en el modo fecha y hora del calendario, el título se parte en dos renglones** al lado de «Cerrar» (costo de este cambio, aceptado y declarado en el README de d05).
- **En el mapa compartido, «Cerrar el mapa» no dice de qué mapa** (H6-13, `MENOR`): con los dos mapas de «Contacto» abiertos, el lector de pantalla oye dos botones con el mismo nombre (`ubicacion-picker.html`, primer estado). Las copias del alta sí lo dicen, porque su nombre está escrito en la plantilla. Arreglarlo en el componente compartido pide un rótulo más por consumidor; queda para la parte de D-05 del médico, que toca los otros consumidores.
- **`playwright/mi-perfil-paciente.mjs:93-94`** (guion de Pablo) afirma que «Editar» es un lápiz sin texto; con H6 esas dos comprobaciones quedan viejas. Se avisa en el PR de H6.

- **Una descarga después de recargar trae `{}` en el simulador** (ajeno, no se toca): el simulador de archivos guarda los bytes subidos en memoria (`core/mock/handlers/files.handlers.ts:32`, `bytes?: Blob`), la colección se persiste con `JSON.stringify` (`core/mock/mock-store.ts:197`), que convierte el `Blob` en `{}`, y `/common/files/:id/content` devuelve eso (`files.handlers.ts:219`). Antes de recargar, la descarga trae el archivo exacto; el vínculo del archivo sí sobrevive a la recarga. Afecta a cualquier pantalla del simulador que suba y vuelva a bajar un archivo.
- **A 375 la página del editor se pasa 5 px en todas las pestañas** (heredado): lo que se sale es `div.app-header__derecha` del encabezado de la app, el mismo elemento que en «Mi perfil» (`evidencia/h4/desborde-375.txt`). Las tablas y la fila de pestañas se recorren dentro de su propio contenedor.
- **Los filtros del listado de pacientes de administración se ven cortados** (visto al recorrer los consumidores de la barra, `evidencia/h3/capturas/h3-barra-pacientes.png`): fuera del diff.

## Decisiones y ambigüedades

- **Direcciones obligatorias (D-06)**: en la aseguradora y en la central de laboratorio e imagenología la dirección es obligatoria; al vaciarla el mapa, el campo muestra además su error de obligatorio, y en la aseguradora el aviso queda junto al mapa porque entre el campo y el mapa está «Nombre comercial». El #606 lo dejó como observación; la segunda pasada de capturas lo califica `MAYOR` del cambio (H5-01, H5-02) y las tres pantallas quedan `RECHAZADA`. Queda como H5.S2.M6, a la espera de una decisión de producto: no marcar en rojo el campo hasta que la persona lo toque o intente avanzar, y acercar el aviso al campo. A confirmar con Itzan.
- **Consola en el guion de H5**: se excluyen de forma explícita, y se cuentan, los avisos de CSP por scripts en línea que aparecen en todas las rutas, incluido el inicio de sesión que H5 no toca (medido en la misma sesión; condición documentada en `src/server/security-headers.ts:150-160`). Cualquier otro error de consola hace fallar el guion.
- Q-I11 (registrada en `PLAN.md`; no bloquea): las flechas del formulario por páginas que encienden las altas siguen como excepción del ADR-0012 §3 (pasar de página), con su justificación al lado. Q-19 nombra al paginador de listas (`molecules/pagination`, de Pablo), no a esta navegación. Si el doctor las quiere con texto, alcanza con quitar `[iconOnlyNav]` en cada alta. A confirmar con el doctor y Pablo.
- Q-I12 (registrada en `PLAN.md`; no bloquea): el calendario pasa a ícono + texto porque la excepción exige un globo visible y ahí queda tapado (hallazgo del globo, arriba). Si el átomo llega a funcionar dentro de un modal, podrían volver a la excepción. A confirmar con Pablo.
- Q-17 (ruta y renglón de «Mis puntos»): el renglón ya salió del menú y la ruta vieja ya redirige, las dos cosas del 23/09 y de otro carril; la redirección va a `/my-account` y no a la pestaña (hallazgo en «Riesgos residuales»). El pedido de H7.S1.M3 sigue siendo el destino `?pestana=puntos`.
- Q-I3 (registrada en `PLAN.md`, tabla de ambigüedades; bloquea H2.S3.M5): `pestanas-del-perfil-medico.spec.ts:64` fija el conjunto exacto `['password','sexAtBirth']`; «el spec pasa sin tocarlo» no puede ser literal.
- Q-I6 (registrada en `PLAN.md`; bloquea H2.S2.M5): qué pasa con `specialtyPrimary` al retirar el select de la principal. Supuesto: desaparece del alta y del mapa; el spec no se toca.
- El pedido de H7.S1.M3 no se escribe en el daily de Ender (es su archivo): Itzan se lo entrega como documento aparte y Ender completa su propia fila. Decidido por Itzan el 23/09.
- Q-I8 (registrada en `PLAN.md`; no bloquea): el encargo pide confirmar al agregar un título y el ADR-0015 exime el alta. Supuesto: se confirma, porque manda el encargo de esta tarea.
- Q-I9 (registrada en `PLAN.md`; bloquea H4.S3.M4): el alto máximo de la tabla pliega las columnas secundarias también en escritorio. Supuesto: se mide y, si la institución o la emisión dejan de verse en la fila de títulos, no se activa en esa tabla.
- **Correcciones en piezas de Pablo (H3.S4)**: Pablo aprobó las tres correcciones propuestas (clave de búsqueda configurable en `app-filter-bar`; historial laboral en modo tabla conectado a `updateAffiliation`/`removeAffiliation`; `confirmarDescarte()` en el historial) y las encargó a este carril el 23/09. Eso levanta la exclusión del encargo sobre `organisms/filter-bar/**` y `work-history/**` sólo para ese alcance; el resto de sus archivos sigue fuera. El total del plan pasa de 96 a 103 microtareas.
- Q-I10 (H3.S4.M5): el historial en modo tabla adjuntaba un archivo al vínculo, pero el contrato de afiliaciones no tiene dónde guardarlo. **Decidido por Itzan (24/09): se quita el adjunto** hasta que el contrato lo tenga. Se verificó en las tres capas antes de quitarlo: la tabla `profiles.practitioner_affiliations` del modelo no tiene columna de archivo, ni el DTO ni la entidad de la API, ni `PractitionerAffiliation` / `UpdatePractitionerAffiliation` en el cliente; y el vínculo genérico `common.file_links` sólo acepta como dueño `USER`, `PATIENT`, `TENANT`, `CONDITION` o `PROCEDURE` (`src/modules/common/dto/enums.ts:8-16` de la API). Para que vuelva, la API tendría que sumar un dueño de tipo afiliación a `file_links` o un `file_id` a la tabla. Queda para Pablo, que lleva ese contrato.
- **Q-9 decidida (a), por Itzan el 23/09**: el respaldo de una especialidad es un título ya cargado, vinculado con `supportingCredentialId` en el alta, que es el único camino que el contrato admite. Sólo se ofrecen títulos verificados, porque la API rechaza uno pendiente (422). El vínculo no se muestra ni se corrige después, porque ni la lectura ni la corrección lo traen: queda declarado como límite del contrato.
